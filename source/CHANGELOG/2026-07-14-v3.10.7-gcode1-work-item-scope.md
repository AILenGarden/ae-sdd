# 2026-07-14 | ae-sdd v3.10.7 - scoped G-CODE-1

- G-CODE-1 复用 G-09 的 VerificationPlan、路径 containment、fingerprint、evidence manifest 与 artifact hash 校验。
- 可信 scope 仅扫描当前 work-item 生产代码；scope 内 blocker 阻断，scope 外历史债不阻断。
- 缺失/空 scope 保持全仓严格扫描；测试/文档-only scope fail-closed。
- scoped 路径不读取或创建 baseline；未创建任何 baseline。
- 回归覆盖 scoped clean、新 blocker、触及历史债、plan/manifest/artifact 篡改、路径越界与 full-repository fallback。
