# 2026-07-14 | ae-sdd v3.10.8 - G-CODE-1 fail-closed attestation

- G-CODE-1 仅在 VerificationPlan、当前 G-09 evidence、项目内相对 artifact 与 Story/scope/command/toolchain/report 三方绑定全部可信时启用 work-item scope。
- scanner 缺失、非法 exit/status、未知 finding severity、不安全 finding path、测试/构建产物路径或大小写扩展名分类漂移均阻断。
- scanner JSON 增加 `scannedPaths`；gate 校验路径安全、唯一性、production eligibility 与 scope 完整覆盖。
- scanner report 的 root、codeFiles、codingReports、reportStats 与 findings 必须精确自洽，缺失、类型错误或计数漂移均 `BLOCK_SCAN_INVALID`。
- 无可信 scope 保持严格全仓扫描；scoped 模式不读取或创建 baseline。
- TDD targeted 最终 29 passed / 60 subtests；wide 203 passed / 92 subtests；六轮独立 reviewer 最终 CLEAN。
- 修复 Maven `pom.xml` 标准 namespace/schemaLocation 被误判为硬编码 endpoint；豁免由 XML 解析与精确标准值共同约束，真实 Java/XML 外部 URL 继续阻断。追加组合回归 194 passed / 92 subtests。
