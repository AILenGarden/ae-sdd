# 2026-07-13 | ae-sdd v3.10.4 - Installed runtime G-PATH exclusion

## Fixed

- G-PATH now excludes compiler-generated `runtime/` compact views when the scan root is an installed compiled package (`runtime/manifest.json` present and no `source/` directory).
- Authoritative package inputs such as `skills/`, `skill-fallbacks/`, `standards/`, and `templates/` remain in scope, so real hard-coded path violations still block.

## Verification

- Reproduced the installed/no-`AE_SDD_MASTER` failure with five violations from `runtime/skills/cross-cutting/document-storage-skill/outline.compact.md`.
- `TestGPath`: 15 tests passed, including generated-runtime exclusion and real `skill-fallbacks/` violation coverage.

## N/A

- No gate registry, regex, CLI contract, source/master scan, project-memory scope, design model, architecture boundary, or release-package structure changed.
