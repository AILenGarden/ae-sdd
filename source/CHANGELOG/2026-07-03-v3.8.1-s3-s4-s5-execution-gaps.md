# 2026-07-03 | ae-sdd v3.8.1 - 执行层缺口补全（S-3/S-4/S-5 + B-2/B-3 核实）

## Summary

基于 `update-doc/2026-06-26-ae-sdd-automation-gap-analysis.md` 的 12 项缺口逐项核实代码现状，发现该缺口文档已显著过时——12 项中 6 项（B-1/B-2核心/B-3/S-1/S-2/O-3）均已落地，commit `9be4b92` 的"B2/B3 remediation"属实。本次聚焦剩余 4 项真实缺口：(1) S-3 多 sub-agent 并发写冲突检测空白；(2) S-4 `health` 缺规则-工具同步状态检查（原 item 9 仅比版本号，无法检测同版本内漂移）；(3) S-5 `prd-complete --runtime` 接收参数但从不分支、`state.py` 无 `prd_complete`；(4) B-2 残余（核实后亦已落地，仅留文档记录）。本次新增 `state.fileLocks` 文件意图锁 + hook 冲突告警、`tools/.sync-manifest.json` 构建期生成 + health 第 10 项漂移检测、`state.prd_complete` runtime 差异化 helper（mavis/claude-code/codex 三分支）。

## Changes

| Area | Change |
|---|---|
| state.py | 🆕 S-3 新增 `fileLocks` 字段 + `acquire_file_lock`/`release_file_lock`/`check_file_lock` 3 helper（TTL 30min 惰性失效，幂等获取，仅持锁者释放） |
| state.py | 🆕 S-5 新增 `prd_complete(state, prd_id, runtime, project_root)` helper：生成 summary.md + 流转 prdStatus + runtime 差异化（claude-code 写 compact-trigger 文件） |
| state.py | docstring 补 `fileLocks`/`activeAgents`/`agentReports` 字段说明 |
| gate_intercept.py | 🆕 S-3 `_check_product_landing` 内新增文件锁冲突告警（≥2 活跃 agent 且目标文件被锁时 warn，首版不阻断） + `_relative_product_path` 辅助 |
| ae-sdd (CLI) | 🆕 S-3 新增 `state lock`/`state unlock` 子命令（--path/--agent/--ttl） |
| ae-sdd (CLI) | 🆕 S-5 `cmd_state_prd_complete` 改为调 `state.prd_complete` 并按 runtime 分支；`cmd_runtime_compact` 补 summary.md 生成 |
| ae-sdd (CLI) | 🆕 S-4 `cmd_health` 新增第 10 项"规则-工具同步状态"，读 manifest 比对 hash，漂移 warn_only 不阻断退出码 |
| update_graph.py | 🆕 S-4 新增 `generate_sync_manifest`/`write_sync_manifest`/`check_sync_drift`/`sync_manifest_path` 4 函数 |
| build_dist.py | 🆕 S-4 构建期调 `update_graph.write_sync_manifest` 生成 `tools/.sync-manifest.json` 随分发 |
| .gitignore | 新增 `tools/.sync-manifest.json` 忽略（构建产物，不入库） |
| SKILL.md | 工具速查表新增 `state lock/unlock` + health 标注 10 项；多 Agent 段新增 S-3 fileLocks 承诺；PRD 段新增 S-5 runtime 差异化表 |
| test_state.py | 🆕 新增 `TestFileLocks`（6 用例）+ `TestPrdComplete`（5 用例） |
| test_update_graph.py | 🆕 新增 `TestSyncManifest`（4 用例：结构/一致/漂移/缺失兜底） |

## 触发原因

- **S-3**：gap 文档 🟠 严重项——`write_state` 仅原子写无锁检查，多 sub-agent 并发写同一 state.json/产物会丢更新；规则仅文档无工具强制
- **S-4**：gap 文档 🟠 严重项——`health` item 9 master-freshness 只比版本号字符串，无法检测"同版本内规则-代码漂移"（SKILL.md 声明 gate 但 gates.py 未实装）；无 `.sync-manifest.json`
- **S-5**：gap 文档 🟠 严重项——`cmd_state_prd_complete` 接收 `--runtime` 但从不分支（行 371 仅 echo）；`state.py` 无 `prd_complete()`；mavis/claude-code/codex 差异化行为未实现
- **B-2/B-3 核实**：原 gap 文档判定未实装，核实后均已落地（B-2 `gates.py:1706` 用 `paths.resolve_doc_workspace`、B-3 `stop_check.py:160-262` 完整实现+8 测试），本次仅留 CHANGELOG 记录核实结论

## 影响范围

- 涉及运行时逻辑：S-3 hook 冲突告警（新增 warn 分支，不改现有拦截链）、S-5 prd-complete 行为变化（新增 summary.md 生成 + runtime 分支，旧调用方参数不变向后兼容）
- 涉及 CLI 新命令：`state lock`/`state unlock`（新增子命令，不影响现有）
- 涉及构建产物：`tools/.sync-manifest.json`（build_dist 生成，gitignore，不入库）
- 不改变已有门禁 ID/子 SKILL 职责边界/文档存放路径
- 版本号：本次为 v3.8.1 增量，未改 SKILL.md frontmatter version（沿用 v3.8.0，避免触发 UC-01 三处同步；若需正式发版再 bump）
- 无破坏性变更：fileLocks/sync-manifest 缺失时均兜底放行（旧环境兼容）

## 验证方式

- `python -m pytest tools/tests/ -q` → 901 passed, 1 skipped（含新增 15 用例：S-3×6 + S-4×4 + S-5×5）
- `python tools/bin/ae-sdd health` → 第 10 项"规则-工具同步状态"显示 ✅ 全部一致（manifest 生成后）或 ⚠️ N/20 漂移（改动后未 regen manifest 时，warn_only 不影响退出码）
- `python tools/bin/ae-sdd state lock --help` / `state unlock --help` → 子命令已注册
- S-3 helper 烟测：`acquire_file_lock` 冲突返回持锁 agentId；TTL 过期可抢占；仅持锁者能 release
- S-5 helper 烟测：3 runtime 均生成 summary.md；claude-code 额外写 compact-trigger；prdStatus → awaiting_compact

## Reviewer

陈聪
