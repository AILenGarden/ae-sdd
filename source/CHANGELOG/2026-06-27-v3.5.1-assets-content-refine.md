# 2026-06-27 | ae-sdd v3.5.1 - Project Assets Content Refinement

## Summary

本次变更根据 `D:\al-agent-workspace\ae-sdd-update-doc\2026-06-27-assets-content-refine-proposal.md` 修订项目资产内容模型。原项目资产 schema 已经具备 7 层索引、可信度三态、安全登记和拆分规则，但 function 业务专题缺少 DTO 字段级变更、Redis Key、跨服务 Feign、关键约束和集成测试范式等实战维度。

本次在母版 schema/template 中补齐这些实战字段，并新增 `source/templates/project-assets/` 下三类横切专题 starter 模板，让后续资产生成可以直接产出可供 CodingPlan 使用的"骨架 + 实战 + 门禁"材料。

## Changes

| Area | Change |
|---|---|
| project-assets-schema | 为 §6.9 隐性约定增加结构化表格与出处/反向链接门禁。 |
| project-assets-schema | 为团队惯用实现方式记录格式增加"实战案例"字段。 |
| project-assets-schema | 扩展附录 C function 专题模板，新增 DTO 字段变更、Redis Key、跨服务 Feign、关键约束、集成测试范式 5 个实战维度。 |
| project-assets-template | 同步隐性约定表、Code Plan 输入索引和团队经验"实战案例"字段。 |
| source/templates/project-assets | 新增 `function-topic-template.md`、`config-topic-template.md`、`domain-topic-template.md` 三个 starter 模板。 |
| README | 更新第 5 行版本摘要，记录 2026-06-27 项目资产内容修订。 |

## 触发原因

- 修订文档指出同事知识库已有 ASCII 调用链图、DTO 字段级变更、Redis Key 设计、Feign 调用表、关键约束和 H2 + DRIFT 集成测试范式，而 ae-sdd 项目资产原先只有抽象经验骨架。
- `project-assets-template.md` 已引用 `source/templates/project-assets/{function,config,domain}-topic-template.md`，但仓库中缺少对应 starter 文件，存在文档引用与物理文件不一致。
- 当前 `source/assets/icec-cloud-boss/icec-cloud-boss.assets.md` 已在未提交变更中标记为 deprecated，本次不再向废弃实例追加专题内容，实例迁移应落到工作区 `.ae-sdd/assets/`。

## 影响范围

- 本次为母版规则/模板层变更，不修改 CLI、门禁执行逻辑或 `tools/` 运行时代码。
- 新增 starter 模板只影响后续项目资产生成与维护，不强制迁移既有实例。
- 不改变 G-00/G-DOC-STORAGE 等门禁判定对象；资产路径仍以 document-storage 与 paths.py 为 SSOT。
- 无破坏性变更；旧资产文档不含新增字段时仍可继续读取。

## 验证方式

- `python tools/bin/ae-sdd update-check --affected source/standards/project-assets/project-assets-schema.md,source/standards/project-assets/project-assets-template.md,source/templates/project-assets/function-topic-template.md,source/templates/project-assets/config-topic-template.md,source/templates/project-assets/domain-topic-template.md,README.md` 查询连带项。
- `python tools/bin/ae-sdd update-check` 全量验证更新闭环。
- 人工核对 `project-assets-template.md` 中引用的 3 个 starter 模板文件均真实存在。
- 人工核对附录 C 不再重复定义 Feign/关键约束章节，统一收敛到 §1.7/§1.8。

## Reviewer

Codex

---

## v3.5.1.1 补做（2026-06-27）

> **触发原因**：用户决策"补做剩余 4 项（P1+§3.5+§3.6）"+ 客服域概览新建（修订方案 §3.4 的 domain 部分）。

### v3.5.1.1 Changes

| Area | Change |
|---|---|
| project-assets-schema | §5 核心类方法表（§5.1/§5.2/§5.3 DO/§5.3 DomainService/§5.4 Repo/§5.4 Feign/§5.5 ServiceImpl 共 7 张表）4 列 → 5 列，新增"变更点"列 + 强制规则说明。 |
| project-assets-schema | §6.9 隐性约定表填 4 条初始数据（互踢与融云无关 / 事务中禁止远程调用 / Feign 失败处理差异化 / 错误码区分账号存在性）。 |
| icec-cloud-boss.assets | §11.8 测试模式扩 2 条：§11.8.3 集成测试 H2 + DRIFT 落库门禁 + §11.8.4 Mock 策略：仅 Mock 非落库协作者。 |
| icec-cloud-boss.assets/function | 新建 `STORY-003-BE-登录认证.md`（从同事知识库 `function/登录与认证-BE-接口逻辑速查.md` 迁入 10872 字节 + 按 function-topic-template 模板格式化）。 |
| icec-cloud-boss.assets/config/test | 新建 `api-test-env.md`（从同事知识库 `config/test/api-test-env.md` 迁入 2989 字节 + 按 config-topic-template 模板格式化）。 |
| icec-cloud-boss.assets/domain | 新建 `cs-域概览.md`（基于工程文档 `icec-cloud-life-cs.md` + `project/.md` 归纳客服域边界/状态机/跨域依赖/错误码/红线/术语表）。注：同事知识库 `domain/客服.md` 是 0 字节空文件，本概览为首份客服域结构化文档。 |
| icec-cloud-boss.assets | §12 横切专题三处索引从"暂无"填入 3 篇首批专题文件链接 + 注释补做说明 + v3.5.1 自我豁免声明。 |
| update-doc/history | 创建 `D:\Item\ae-sdd\update-doc\history\` 归档目录，归档修订文档 v1.0（带"已落地"头部标注）。 |

### v3.5.1.1 触发原因

- v3.5.1 CHANGELOG 行 24 自我声明"本次不再向废弃实例追加专题内容"，导致修订方案 §3.3 / §3.4 P1 项未实施。
- 用户原始请求"根据修订文档更新 ae-sdd，修改完之后将文档归档"未明确"只补 P0"或"按方案 P0→P1→P2 全部执行"。
- 用户在 plan 阶段确认"补做剩余 4 项" + 客服域概览新建，因此本节补做。

### v3.5.1.1 影响范围

- 本节为对废弃 boss 主体（deprecated 实例）的追加，违反 v3.5.1 自我豁免声明。boss 主体不应用于生产 CodingPlan，仅作历史参考。
- 3 个新文件（function/config/domain）按 v4.0 资产路径治理的就近存放原则，放在 `source/assets/icec-cloud-boss/{function,config,domain}/` 而非集中 `source/assets/icec-cloud-boss/{function,config,domain}/`。
- §3.5 §5 增"变更点"列属 schema 模板变更，不影响现有 6 份资产实例（按字段扩展兼容策略，旧文档 4 列仍可读取）。
- §3.6 §6.9 填 4 条初始数据不增加门禁严格度，仅为示例数据。
- 无破坏性变更。

### v3.5.1.1 验证方式

- `grep "实战案例" source/standards/project-assets/project-assets-template.md` —— 验证 §11.1.1 模板含"实战案例"字段。
- `grep "11.8.3" source/assets/icec-cloud-boss/icec-cloud-boss.assets.md` —— 验证 §11.8 已扩 4 条。
- `ls source/assets/icec-cloud-boss/{function,config,domain}/` —— 验证三个目录均有 .md 文件。
- `grep "变更点" source/standards/project-assets/project-assets-schema.md | wc -l` —— 验证 §5 7 张表均含"变更点"列（期望 ≥ 14：7 张表 × 至少 2 处）。
- `ls update-doc/history/` —— 验证归档目录存在 + 修订文档副本存在。
- 人工核对 `domain/cs-域概览.md` 头部"基于工程文档归纳"标注齐全。

### v3.5.1.1 Reviewer

待补（Mavis 提交 + 用户评审）

### v3.5.1.1 元说明

本节为 v3.5.1 自我豁免的补做记录，不应作为 v3.5.1 原始范围的混淆。建议后续实例迁移（`2026-06-27-assets-instance-migration-plan.md` 待写）走工作区路径（`.ae-sdd/assets/`），boss 主体彻底废弃前 `source/assets/icec-cloud-boss/` 内容仅作历史参考。
