# 2026-07-14 | ae-sdd v3.10.6 - Story entry and scoped G-09

## Fixed

- G-13 now exempts only the DR dependency for `entryNode=STORY` with medium scale. DR/PRD, legacy/no-entry, and large routes remain strict; mature Story/Task/CodingReport/CodeReview links remain required.
- G-09 now uses verified `VerificationPlan.changedPaths` first, with compatible state changed-path fallbacks. Invalid, missing, or out-of-project scoped paths fail closed; no scope preserves strict full-repository behavior.
- G-09 no longer reads or returns G-CODE-1 baseline state. No baseline is created by this change.

## Integrity

- Evidence manifests now carry a canonical content hash. Current G-09 evidence must match Story, plan input fingerprint, successful exit state, and artifact SHA-256; evidence remains provenance/cache, never a waiver.
- `ae-sdd verify plan --work-item <ID> --persist` validates project-contained files and atomically persists a Story/work-item-bound plan; omitted `--persist` is read-only.
- `ae-sdd evidence finalize --story <ID>` upgrades unhashed manifests, verifies artifact SHA-256, and refuses existing manifest or artifact hash mismatches.

## Verification

- `tools/tests/test_g09_g13_work_item_scope.py`: 16 passed, 13 subtests passed.
- `tools/tests/test_gates.py`: 146 passed, 19 subtests passed.
- Persist/finalize + scoped gate set: 28 passed, 13 subtests passed.
- Full `tools/tests`: 1303 passed, 6 skipped, 32 subtests, 7 unrelated failures (3 pending harness/UC-07 distribution refresh, 3 pre-existing Windows GBK quick-channel failures, 1 legacy Task-phase transition expectation).
- `ae-sdd update-check`: 17/18 pass before distribution; UC-07 alone waits for build/install to refresh `.harness/.adapter.lock`.

## N/A

- No scanner rules, G-CODE-1 baseline, external endpoint, distribution files, or installed runtime were changed directly.
