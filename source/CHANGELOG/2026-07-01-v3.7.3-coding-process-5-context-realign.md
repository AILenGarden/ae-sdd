# 2026-07-01 | ae-sdd v3.7.3 - CodingProcess 5 上下文重排 + 统一走 document-storage

## Summary

CodingProcess 的 5 上下文改为「项目约束、技术约束、Story、Task、TestCase」，替换掉原来的第⑤项「项目资产」（项目资产读取已下沉到 §A2 调 coding-skill 能力时的内部步骤，不再作为独立上下文重复声明）。5 上下文的读取全部改为显式调用 `document-storage-skill` 的 API（`get_constraints` / `get_thinking_engine` / `resolve_path`），不再手写路径或裸调 `ae-sdd assets read`。

## Changes

| Area | Change |
| --- | --- |
| coding-process-skill.md §CodingProcess.Run 对外契约 | 5 上下文表：③④⑤ 从"当前 Story（微任务无）/ resolve_path(intent=TASK) / ae-sdd assets read"改为统一 `document-storage-skill.resolve_path(intent=STORY/TASK/TESTCASE, ...)`；第⑤项目资产→TestCase 文档 |
| coding-process-skill.md §A1 加载5上下文表 | 同步改写，①②加 `document-storage-skill.` 前缀；③④⑤改resolve_path 调用+补一行"项目资产已下沉"说明 |
| coding-process-skill.md §B1 工程预检 | "收集输入"从"向开发者请求路径"改为"复核 §A1 已加载的 5 上下文"，避免 Execute 阶段重复索取一遍 |
| coding-process-skill.md §执行清单 A1 | "约束/CodingModel/Story/Task/项目资产"改为"项目约束/技术约束/Story/Task/TestCase，均经 document-storage-skill 定位" |
| SKILL.md | frontmatter version 3.7.2→3.7.3 + description 追加本次变更说明 |
| paths.py / README.md:5 | 版本号三处同步 |

## 触发原因

用户澄清 CodingProcess 的 5 上下文构成："项目约束、技术约束、Story、Task、TestCase"，并要求"读取 ae-sdd 生成的文档时，请统一请求 document-storage-skill"。原文档第⑤项写的是"项目资产"，与 TestCase 系列独立成链（v3.7.0 引入）后的实际输入不一致；且 ③④⑤ 三项存在部分手写路径（"当前 Story"、裸调 `ae-sdd assets read`），未走 document-storage-skill 统一定位。

## 影响范围

- 仅 `source/skills/phase2-coding/coding-process-skill.md` 内部 4 处表格/描述改写，不涉及门禁注册、CLI 命令、state 机器字段——`tools/lib/gates.py`、`state.py`、`gate_intercept.py` 均已在 v3.7.0 TestCase 系列引入时补齐 testcase-generated/testcase-reviewed phase，无需再改。
- G-CODEPLAN-SRC / G-14 / G-08 门禁不受影响（仍校验 CodingPlan 产出物，与上下文加载方式无关）。

## 验证方式

- `ae-sdd update-check` 全绿
- `python tools/tests/test_state.py` / `test_gate_intercept.py` 通过（未改动 state 机器，仅改文档表述）

## Reviewer

陈聪
