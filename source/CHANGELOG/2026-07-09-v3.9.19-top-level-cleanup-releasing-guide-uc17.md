# 2026-07-09 | ae-sdd v3.9.19 - 顶层结构整理 + 发版包指南 + UC-17 级联守门

## Summary

仓库顶层积累了 scratch 噪音（`nul` / `_tmp_*.py` / `README.docx` / `update-doc/` / `logs/`），且 README §📦 仓库结构树与实际 28 项脱节（只画了 6 项，漏 `tools/`/`apps/`/`docs/`/`plugins/`/`references/`），让人误以为这些杂物会进发版包。

核实后发现核心目标——「发版包只要 ae-sdd 本身」——**其实早已满足**：`build_dist.py` 只从 `source/`（剔除 CHANGELOG/docs/.idea）+ `tools/`（剔除 tests/）取材，`dist/ae-sdd/` 固定 13 项全是本体，零杂物。所以本次是配套治理而非改发版包内容：清理顶层 scratch、补齐 README 结构树、新建独立的 `RELEASING.md` 发版包指南，并把「顶层结构 = 发版包边界」这条纪律固化进 update-graph，让 `ae-sdd-update-skill` 经新 UC-17 自动级联校验——保证以后每次结构改动都不影响发版包结构。

## Changes

| Area | Change |
|------|--------|
| 顶层 | 删除 scratch：`nul`（Windows 保留设备名误产物）、`_tmp_mark1.py`/`_tmp_mark2.py`/`_tmp_norm.py`（临时脚本）、`logs/`（旧 sync 日志）、`README.docx`（25KB 二进制，与 README.md 重复且无引用） |
| 顶层 | `update-doc/history/` 4 份归档 plan 经 `git mv` 归并到 `docs/plans/`（保留 git 历史 + CHANGELOG 溯源链），删除空的 `update-doc/` |
| README.md | §📦 仓库结构 ASCII 树补齐 `tools/`/`apps/`/`docs/`/`plugins/`/`references/`，新增「🚧 发版包边界」核心规则声明（只从 source/+tools/ 取材，其余顶层目录一律不进 dist） |
| RELEASING.md | 新建顶层发版包指南（5 节）：构成表 / 发版包边界（取材规则 + 顶层目录归类表 + 安装链路）/ 版本策略（SemVer + 三处同步 + CHANGELOG 一版一文件）/ Cut a release 步骤 / 改顶层结构后必须做什么 |
| update_graph.py | 新增 `check_uc17_repo_layout_contract`：校验顶层无 scratch 残留 + README 标注发版包边界；注册到 `CHECK_FUNCS["UC-17"]`。Windows `nul` 保留设备名用 `os.listdir` 判定避免跨平台误报 |
| update-graph.json | 新增 UG-23「仓库顶层结构变更」规则（trigger: README.md/RELEASING.md/.gitignore → checks: UC-17） |
| ae-sdd-update-skill.full.md | UC 检查表补 UC-16（既有遗漏）+ UC-17 行；图谱维护规则补「改顶层结构」级联提示指向 RELEASING.md §5 |
| paths.py | 修正既有版本漂移 `MASTER_VERSION` 3.9.11 → 3.9.12（与 SKILL.md 对齐，UC-01 历史债） |
| test_update_graph.py | 同步 check_all 计数 16→17、manifest 规则数 21→22；新增 TestUC17 四用例（real-pass / scratch 残留 fail / README 边界缺失 fail / nul 设备名不误报） |

## Verification

- `bash scripts/build-dist.sh` → `dist/ae-sdd/` 仍 13 项（SKILL.md/HARNESS.md/VERSION/assets/docs/harness/runtime/scripts/skill-fallbacks/skills/standards/templates/tools），零杂物（standalone-skills/references/apps/update-doc/README.docx/logs 全 absent）
- `ae-sdd update-check`（CLI 真实路径）→ 17/17 全 pass（UC-17 顶层结构契约 ✅ + UC-14 三方一致：22 UG / 17 UC）
- `pytest tools/tests/test_update_graph.py test_standalone_skill_runtime_compiler.py test_iteration_check.py test_cli_state_work_item.py` → 95 passed, 5 skipped

## Notes

- `standalone-skills/` 保留顶层未移动：它不进 dist，且被 6 处代码硬编码引用（compile_all_skills.py / update_graph.py / test / update-graph.json），移动属有代码风险改动且与「发版包只要 ae-sdd 本身」核心目标无关。
- 本次为仓库级结构整理，非功能 bump；CHANGELOG 走 v3.9.19 一版一文件。
