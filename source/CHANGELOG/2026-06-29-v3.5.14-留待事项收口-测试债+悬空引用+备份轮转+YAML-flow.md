# 2026-06-29 | ae-sdd v3.5.14 留待事项收口 - 测试债 + 悬空引用 + 备份轮转 + YAML flow

> **版本号：** 3.5.14（工具链 + 文档一致性，不 bump 版本号——SKILL.md frontmatter 已含 version 字段）
> **性质：** 🛠️ 工具链修复 + 📄 文档一致性（修订建议书评审报告 §"留待后续"4 项收口）
> **影响范围：** plugin_loader.py + build_harness.py + SKILL.md frontmatter + 14 子 SKILL 悬空引用 + 4 测试文件
> **来源：** `2026-06-29-修订建议书评审结论+处置报告.md` §"留待后续"①②③④ 项

---

## Summary

上一轮（commit 33e192b）落地了🅰第一批 7 项后，评审报告列了 6 项"留待后续"。本次按用户确认执行其中 4 项（⑤D7 mavis daemon 端 + ⑥life git init 跳过）：

- **④ AA test-order**（test_summarize 7→13）：根因是 alignment_audit.py:666 `register_to_update_graph()` 在 import 时自动把 UC-08~13 注入共享 `ug.CHECK_FUNCS`，导致全量 pytest 收集 test_alignment_audit 后 test_update_graph 拿到 13 而非原生 7。修法：test_update_graph 显式 import alignment_audit 使行为确定，断言改 13 + 注释说明耦合。
- **② K2 harness bak 轮转**：build_harness.py:412 每次构建 `shutil.copy2` 一个 `.bak.<ts>` 无清理 → 30 个永久累积。新增 `cleanup_old_bak(keep=3)` 备份后调用，按 mtime 降序保留最近 3 个。实测 30→3。
- **③ E3 YAML flow-style**：`_parse_yaml_subset` 显式声明不支持 flow style，`plugins: []` 被解析成 str `'[]'`。在 `_coerce_scalar` 加 `[]`/`{}` 识别 + `_split_flow` 嵌套安全切分。附带发现 e2e test 失败的真正根因是 SKILL.md frontmatter 缺 `version:` 字段（version 在 body L13），导致 plugin_loader `_read_master_version` 只扫 frontmatter 返回 None → 兼容性警告不发。修法：把 `version: 3.5.14` 移入 frontmatter（同时治 SSOT H8 主 SKILL frontmatter 缺 version）。
- **① H1 悬空引用**：`ae-sdd-skill.md` 在 v3.0 已删除并入 SKILL.md，但 62 处功能性引用仍指向它。Python 精确分类 A 类（62 真悬空）/ B 类（27 历史标记），A 类全替换为 `SKILL.md`，B 类（"原 ae-sdd-skill"/"🆕 v3.0"/"迁入"等溯源标记）保留。

## Changes

| Area | Change |
|---|---|
| tools/tests/test_update_graph.py | test_check_all_returns_7→test_check_all_returns_13 + test_summarize 断言 7→13；显式 `import lib.alignment_audit` 使 AA 注册副作用确定（不再依赖测试执行顺序）；加注释说明 import-time 耦合 |
| scripts/build_harness.py | +`cleanup_old_bak(target, keep=3)` 函数（按 mtime 降序保留最近 N 个 .bak，删其余，keep<0 安全无操作）；备份步骤后调用，治 30 个 bak 无限累积 |
| tools/tests/test_build_harness.py | +`TestCleanupOldBak` 5 用例（保留最近 N / 无 bak 返回 0 / keep=0 删全部 / 数<keep 不删 / 负 keep 无操作） |
| tools/lib/plugin_loader.py | `_coerce_scalar` 加 flow-style 识别（`[]`→空 list / `[a,b]`→list 递归 / `{}`→空 dict / `{k:v}`→dict）；+_split_flow 嵌套安全切分（忽略 []/{} 内逗号）；docstring 移除"不支持 flow style" |
| tools/tests/test_plugin_loader.py | +6 flow-style 用例（空 list / list of scalars / 空 dict / dict inline / 文档分隔符回归） |
| source/SKILL.md | frontmatter 加 `version: 3.5.14`（治 plugin_loader _read_master_version 返回 None + SSOT H8）；body L13 冗余 version 行删除（权威值已在 frontmatter） |
| 14 子 SKILL（H1）| 62 处 `ae-sdd-skill.md` 悬空引用 → `SKILL.md`（agent-orchestration/ai-agent-self-audit-checklist/document-storage/proposal/ae-sdd-update/dr-generate/requirement-analysis/story-generate/story-review/coding-report/coding/task-generate/code-review + SKILL.md 自引用）；B 类 27 处历史溯源标记保留 |

## 验证

| 验证项 | 结果 |
|---|---|
| `ae-sdd update-check` | ✅ 13/13 pass（4 warn 既有：HS-3~6 自认降级 + UC-03 fork/run/skill 历史未实现）|
| `pytest tools/tests/` | ✅ **734 passed / 0 failed** / 1 skipped（HEAD 原 3 failed → 0；E3 + AA test-order + e2e 全修）|
| H1 A 类残留 | ✅ 0（仅剩 1 处 B 类抽象表占位 project-assets-update-skill.md:857，有意保留）|
| build_harness bak 轮转实测 | ✅ 30→3（保留最近 3 个）|
| 副本同步 | ⏸️ 仅改母版，dev-sync 由 commit 时 post-commit 自动跑 |

## 留待后续（仍不在范围）

- ⑤ D7 mavis harness mount bug：当前环境 post-commit 分发链四路全绿，不复现；需 mavis daemon 端源码深查，超母版范围
- ⑥ life git init / D:\Item\doc 游离报告：属 life 项目侧，需用户决策（Maven 多模块 .gitignore 盘点）
- SSOT H8 余项：18 个子 SKILL frontmatter 缺 version（本次只补了主 SKILL.md，子 SKILL 的 version 字段批量补留待后续）
- UC-08 的 188 处「门禁/闸」承诺未绑定 G-XX id：warn 不阻断，属 AA 持续治理项

## Reviewer

- 评审执行：主会话（ae-sdd-update-skill §5 步流程）+ 1 个 sub-agent 执行 H1 分类替换
- 用户确认：执行 ①②③④ 四项（2026-06-29）
- 同步状态：仅修改母版 source/ + tools/，dev-sync 由 commit 触发
