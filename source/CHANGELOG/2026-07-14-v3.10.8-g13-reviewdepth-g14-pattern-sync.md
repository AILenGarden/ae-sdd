# 2026-07-14 | ae-sdd v3.10.8 - G-13/G-REVIEW-DEPTH/G-14 patterns 与引用校验同步 v3.10.0/v3.10.1 改版

## Summary

v3.10.0 砍 Task（主产物改为 `{story}.md`）与 v3.10.1 报告原地更新（去掉 `-v*-r*` 版本后缀）两次流程改版时，G-11/G-12/G-05 跟进了 patterns，但下游引用追溯门禁 G-13 / G-REVIEW-DEPTH / G-14 漏改，导致新布局下报告找不到、Task 追溯断裂、引用校验失效或永真。本次统一修复 6 个 bug，并补 8 个回归测试覆盖新布局场景。

根因：现有 194 个测试几乎全用旧 legacy 布局（`design/` 根 + `-task-*.md` + `-v*-r*` 版本化报告），覆盖不到 v3.10.0/v3.10.1 改版后的新主布局，bug 被测试盲区掩盖。

## Changes

| Area | Change |
|---|---|
| gates.py | 新增 `_iter_dr_docs` helper：DR 文档枚举 rglob 递归子目录 + 排除报告类，G-01/G-13/G-DR-CTX 三处共用（DRY，BUG4） |
| gates.py | 新增 `_find_task_docs` helper：Task 追溯优先 `{story}.md` 主产物 + 回退 `-task-*.md`，G-13 复用（BUG3） |
| gates.py | G-13 RA->DR / Story->DR / n_drs 计数由 `design.glob` 改为 `_iter_dr_docs`（rglob），修复子目录 DR 漏判（BUG4） |
| gates.py | G-13 CodingReport/CodeReview 引用追溯 patterns 加 v3.10.1 非版本化主文件，与 G-11/G-12 对齐（BUG1） |
| gates.py | G-13 CodingReport 引用校验修复 v3.10 主布局永真：`task.stem==story_id` 时改校验 task 文件名/段落标记（BUG5） |
| gates.py | G-REVIEW-DEPTH patterns 加 v3.10.1 非版本化主文件 CodeReview.md，修复新布局下降级 skip 失效（BUG2） |
| gates.py | G-14 AC ID 正则加负向边界 `(?<![A-Za-z0-9])`，防 MAC1 被子串匹配为 AC1；AC1/AC-1/AC_1/AC100 仍匹配（BUG6） |
| gates.py | G-14 Story 引用判定收紧：含任何 STORY-xxx ID 时必须命中 current_story，仅完全无 ID 时才允许边界词 `Story` 通过（BUG5 G-14） |
| test_gates.py | 新增 8 个回归测试：TestG13 四个（v3.10.1 主布局报告/Task 追溯/CodingReport 断链/子目录 DR）+ TestG14 两个（AC 子串/通用词绕过）+ TestGReviewDepth 两个（主文件扫描/含证据通过） |

## Notes

- 不新增门禁、不改 GATE_REGISTRY/CHECK_FUNCS（35 门禁计数不变）、不改 CLI 头注释、不改 SKILL.md 命令契约。
- 设计/实现架构文档无能力语义/实现边界变化，N/A。
- 验证：194 原有测试零回归 + 8 新增测试全过；4 个探测脚本 6 个 bug 场景全部翻转。
