# 2026-07-10 | ae-sdd v3.9.22 - 剩余 🟡/🟠 bug 清理 + 门禁列表脱节修复

## Summary

承接上一轮（全盘扫描 + 25 测试转绿 + 6 确定性 bug），本轮处理扫描报告里"留下一轮"的剩余项。用户对 3 组决策拍板后，完成 7 处修复：C2（classify 文档撒谎对齐实现）、C4（G-RA-COMPLETE 门禁列表与真实标题脱节--12 项里 10 项连模板自身都过不了）、C7（YAML 注释剥离切断引号内 #）、B5/C11/C12（Electron monitor Linux 监听失效 + loadFile 未 catch + showOpenDialog null）、C1/C3（死代码清理）。其中 C4 是比预期更深的 bug：G-RA-COMPLETE 门禁的 12 个必填章节锚用 `"§2 角色" in content` 松散子串匹配，但 ra-template.md 真实标题是 `## 2. 角色分析`，两者脱节导致 10/12 项失效。改为标题行正则匹配后，门禁真正生效。B6（recordToolUse JSON 解析）经核实其脚本被 `.github/modernize/java-upgrade/.gitignore` 的 `**/*` 忽略、不入库，撤销本地修改、不在本轮处理。C8/C9（吞异常加日志）经评估跳过：项目无日志基础设施，硬加会引入不一致日志方式，且这些是有意容错设计。

## Changes

| Area | Change |
|---|---|
| tools/lib/gates.py | C1 删除冗余死条件 `or phase == "initialized"`（"initialized" 已在 pre_ra_phases 集合内） |
| tools/lib/document_storage.py | C3 删除 `Path(... if False else full)` 死赋值（三元恒取 full，p 变量未用） |
| tools/lib/document_storage.py | C4 修复 G-RA-COMPLETE 门禁：`_RA_REQUIRED_SECTIONS` 从字符串列表改为 (名称,标题行正则) 元组列表，检查逻辑从松散子串 `"§2 角色" in content` 改为 `pattern.search(content)` 标题行匹配，对齐 ra-template.md 真实标题格式 |
| tools/lib/classify.py | C2 修正 docstring 撒谎：L14"不再硬兜底为对话"改为对齐实现的"低置信度兜底为对话+needs_review"；删除 L344 不可达死分支 `if source == "未知"` |
| tools/lib/paths.py | C7 新增 `_strip_yaml_comment` helper（引号感知注释剥离），read_config 用它替代 `split("#",1)[0]` |
| tools/lib/plugin_loader.py | C7 新增同名 `_strip_yaml_comment` helper，_parse_yaml_subset 用它替代 `split("#",1)[0]`，保留引号内 # |
| apps/ae-sdd-monitor/src/main.js | B5 Linux 不支持 fs.watch recursive:true（异步 error 不进同步 try/catch），改为平台检测：win32/darwin 走 recursive，其他走非递归 |
| apps/ae-sdd-monitor/src/main.js | C11 mainWindow.loadFile() 返回的 Promise 加 .catch，避免进程级 unhandled rejection |
| apps/ae-sdd-monitor/src/main.js | C12 dialog.showOpenDialog 加 `mainWindow || undefined`，防窗口关闭瞬间 mainWindow 为 null |
| tools/tests/test_document_storage.py | C4 补 4 个 G-RA-COMPLETE 测试：全章节通过 / 缺章节拦截 / 正文提及不算章节 / 模板自身通过 |
| tools/tests/test_paths.py | C7 补 test_hash_inside_quotes_preserved：引号内 # 保留 |
| tools/tests/test_plugin_loader.py | C7 补 test_hash_inside_quotes_preserved：引号内 # 保留 |

## 触发原因

- 上一轮全盘扫描报告里"留下一轮"的剩余项，用户要求继续处理。
- C4 深挖发现：G-RA-COMPLETE 门禁列表 `_RA_REQUIRED_SECTIONS` 存的是 `§2 角色` 等简写，但 ra-template.md 真实标题是 `## 2. 角色分析`，松散子串匹配导致 12 项里 10 项（§2~§11）连模板自身都过不了，门禁基本失效。
- C2：classify.py docstring L14 承诺"低置信度 source=未知"但实现兜底为"对话"，文档撒谎（红线 11 变体）。
- C7：自制 YAML parser 的 `split("#",1)[0]` 会切断引号内含 # 的值。
- B5/C11/C12：Electron monitor 的 Linux 监听静默失效、loadFile 未 catch、showOpenDialog null 隐患。
- B6 经核实撤销：recordToolUse.sh/ps1 被 `.github/modernize/java-upgrade/.gitignore` 的 `**/*` 忽略，属本地运行时产物不入库，修改无意义。

## 影响范围

- 代码改动：6 个 lib/工具文件 + 1 个 Electron 应用。其中 C4 改变 G-RA-COMPLETE 门禁行为（从基本失效变为真正校验章节标题存在），是行为增强而非破坏--旧文档若正文恰好含 "§2 角色" 等串能假通过，现在必须有真实标题。
- 测试改动：新增 6 个测试（C4 的 4 个 + C7 的 2 个），无现有测试回归。
- 无破坏性变更：C4 收紧门禁可能让"正文提及但无标题"的 RA 文档不再假通过，但这正是门禁本意。
- 版本号：未推进（仍 v3.9.22，UC-01 一致）。
- 未处理：C5（ra_depth_scan 重复 parse，纯性能优化非 bug）、C6（R′ 正则语义争议，需领域知识确认）、C8/C9（评估跳过，无日志基础设施+有意容错）、C13-C16（install-hooks ls 解析/visited 去重/package.json 跨平台等，影响极小）。

## 验证方式

- `python -m pytest tools/tests -q` -> 1170 passed, 6 skipped, 0 failed（上一轮 1164，本轮新增 6 测试）。
- `python tools/bin/ae-sdd update-check` -> UC-01 版本一致(3.9.22) + UC-07 hash aligned + UC-15 runtime 编译一致，全维对齐通过。
- `node --test apps/ae-sdd-monitor/test/*.test.js` -> 9 passed / 0 failed（Electron B5/C11/C12 无回归）。
- C4 实测：ra-template.md 补齐 RAGeneratePlan/5问/PASS 后通过门禁；缺章节文档被拦；正文提及不算章节。
- C7 实测：`"see #123"` 引号内 # 保留，行尾真注释剥离。

## Reviewer

陈聪
