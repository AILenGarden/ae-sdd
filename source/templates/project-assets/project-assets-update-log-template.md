---
name: project-assets-update-log-template
description: 项目资产更新日志模板 — 记录每次生成/更新/审计的条目。位于 {资产根}/{workspaceKey}.update-log.md（见 document-storage §2.3），配套 project-assets-update-skill.md 使用。
---

# Project Assets Update Log — 项目资产更新日志

> **适用场景：** 记录项目资产（`{projectKey}.assets.md`）的**所有变更**（生成/更新/审计），作为：
> 1. 变更追踪（谁/什么时候/改了什么/为什么）
> 2. 缺口进度可视化（已补/待补/降级）
> 3. 审计依据（每月双源一致性审计的输入）
>
> **精简规则：** 必填章节不得留空；占位符统一 `{花括号}`；表格分隔符统一 `| --- |`；强制规则统一 `> **🔴 强制：**` 引用块。
>
> **配套 SKILL：** [`project-assets-update-skill.md`](../../project-assets-update-skill.md) — 定义何时/如何写本日志。
>
> **🔴 强制：**
> - 每次修改 `{projectKey}.assets.md` **必须**同步本日志
> - "先记录再修改"（防"修完忘记录"）
> - 缺口项不允许直接删除（只能打 ✅ 已补 或 降级）

---

## 0. 元信息 `必填`

| 字段 | 值 |
| --- | --- |
| 项目资产路径 | `{docWorkspacePath}/.ae-sdd/assets/{workspaceKey}/{workspaceKey}.assets.md`（见 document-storage §2.3）|
| 维护人 | `{owner}` |
| 创建日期 | `{YYYY-MM-DD}` |
| 当前 lastAuditedAt | `{YYYY-MM-DD}` |
| 审计周期 | 每月 1 号 |

---

## 1. 状态码约定 `必填`

| 状态 | 含义 | 何时用 |
| --- | --- | --- |
| `🆕 initial` | 首次生成 | 项目资产第一次创建 |
| `⏳ pending` | 已识别变更，待修改 | log 写好"待更新"条目，assets.md 未改 |
| `✅ done` | 已完成 | 变更已落入 assets.md |
| `🔴 blocked` | 卡住 | 探查受阻 / 需用户决策 |
| `⬇️ downgraded` | 降级 | 缺口长期未补，自动降级为低优先级 |
| `🗑️ archived` | 归档 | 撤销项目资产时的备份条目 |

---

## 2. 更新条目 `必填`

> **格式：** 按时间倒序（最新在最上面）。每条 6 字段齐全。

### [{YYYY-MM-DD}] {变更类型} - {一句话描述}

| 字段 | 值 |
| --- | --- |
| **状态** | `⏳ pending` / `✅ done` / `🔴 blocked` / `⬇️ downgraded` |
| **变更类型** | ① 新增微服务 / ② 修改分层 / ③ 补缺口 / ④ 修复错误 / ⑤ 约束变动 / ⑥ 审计发现 / 🆕 首次生成 |
| **涉及章节** | §{N} {章节名} / 多章节用逗号分隔 |
| **变更前** | {变更前的内容/状态} |
| **变更后** | {变更后的内容/状态} |
| **原因/触发** | {Story-ID / 用户指令 / 审计发现 / 探查新发现} |
| **Reviewer** | {谁审的 / 未审} |
| **关联 PR/Commit** | `{git commit hash or PR url}`（如有） |

---

## 3. 异常条目 `选填（条件）`

> 异常路径 A1-A4 触发的条目。结构同 §2，状态为 `🔴 blocked`，且必须在"原因/触发"列说明阻塞原因。

### [{YYYY-MM-DD}] 异常 - {一句话描述}

| 字段 | 值 |
| --- | --- |
| **状态** | `🔴 blocked` |
| **异常类型** | A1 项目结构缺陷 / A2 严重漂移 / A3 长期未补缺口 / A4 用户撤销项目资产 |
| **阻塞原因** | {详细说明} |
| **建议动作** | {如何解锁} |
| **影响范围** | {哪些下游动作受阻（④bis / ⑤ / ⑦）} |
| **解锁后动作** | 走 §{N} {对应动作} |

---

## 4. 缺口进度追踪 `必填`

> **与 `{projectKey}.assets.md` §10 缺口列表联动。** 每月审计时同步此表。

| 指标 | 值 | 趋势 |
| --- | --- | --- |
| 缺口总数 | {N} | - |
| 已补 | {N} | 🟢↗️ / 🟡持平 / 🔴↘️ |
| 待补 | {N} | - |
| 降级 | {N} | - |
| 进度 | {百分比}% | - |

**降级规则：**
- 缺口连续 3 个月未补 → 自动降级为"低优先级"
- 降级不影响功能，但需在 §10 标注"⬇️ downgraded {date}"

---

## 5. 审计报告 `必填`

> 每月 1 号由 [`project-assets-update-skill.md §5`](../../project-assets-update-skill.md) 触发。

### 审计报告 - {YYYY-MM-DD}

| 指标 | 值 | 状态 |
| --- | --- | --- |
| 双源一致性（§6 引用 vs constraints/） | {N}/{total} | ✅/⚠️/🔴 |
| 缺口进度 | {已补}/{总数} ({百分比}%) | 🟢/🟡/🟠/🔴 |
| 知识衰减（包路径/命名/微服务清单） | {N} 处漂移 | ✅/🟠/🔴 |
| 与上次审计的变更数 | {N} 条 | - |
| 已"待更新"未落实条目 | {N} 条 | ✅/⚠️ |
| 触发"重新生成"动作 | 是/否 | - |
| 建议 | {1-3 条具体建议} | - |

**审计签字：** {auditor name}

---

## 6. 历史归档 `选填`

> 已结案的项目资产条目（撤销/重建/超期 1 年）可归档到此章节。

| 日期 | 操作 | 归档路径 |
| --- | --- | --- |
| {YYYY-MM-DD} | 撤销项目资产 | `assets/{projectKey}/.archive/{date}/` |
| {YYYY-MM-DD} | 重新生成（结构重大变更） | 同上 |

---

## 7. 维护 `必填`

- **维护人：** 项目 owner + 架构组
- **更新频率：** 每次项目资产修改（生成/更新/审计）必更新本日志
- **审计：** 每月 1 号审计时同步 §4 缺口进度 + 输出 §5 审计报告

> **🔴 强制：** "先记录再修改" — 在 log 写"⏳ pending"条目 → 修改 `{projectKey}.assets.md` → 改 log 状态为"✅ done"
