---
name: domain-topic-template
description: project-assets domain/ 业务域概览专题 Starter 模板
---

# {业务域名}（{英文 Key}）

> **适用场景：** project-assets `domain/` 业务域概览专题，记录域边界、核心实体与状态机、跨域依赖、错误码与红线、术语表与待确认事项。
> **精简规则：** 必填章节不得留空；占位符统一 `{花括号}`；表格分隔符统一 `| --- |`；强制规则统一 `> **🔴 强制：**` 引用块。
>
> **业务域：** {domain}
> **适用工程：** {工程 A} / {工程 B}
> **最后更新：** {YYYY-MM-DD}
> **可信度：** {已确认 / 据推断}

---

## 1. 域边界 `必填`

| 边界项 | 说明 | 出处 |
| --- | --- | --- |
| 核心职责 | {该域负责什么} | {file:line / Story / 用户确认} |
| 非职责 | {该域不负责什么} | {file:line / Story / 用户确认} |

---

## 2. 核心实体与状态机 `必填`

| 实体 | 状态 / 关键字段 | 说明 | 出处 |
| --- | --- | --- | --- |
| {Entity} | {status / field} | {业务含义} | {file:line} |

```text
{STATE_A} -> {STATE_B} -> {STATE_C}
```

---

## 3. 跨域依赖 `必填`

| 依赖方向 | 依赖对象 | 调用方式 | 约束 |
| --- | --- | --- | --- |
| {本域 -> 外域} | {Service / Topic / DB} | {Feign / MQ / Repository} | {事务/降级/幂等约束} |

---

## 4. 错误码与红线 `必填`

| 错误码段 / 规则 | 含义 | 出处 |
| --- | --- | --- |
| {code range} | {业务含义} | {file:line} |

> **🔴 强制：**

1. **{红线 1}** — {描述} — 出处：`{file:line}`
2. **{红线 2}** — {描述} — 出处：`{file:line}`

---

## 5. 术语表 `选填`

| 术语 | 定义 | 反例 / 易混点 |
| --- | --- | --- |
| {term} | {definition} | {counterexample} |

---

## 6. 待确认事项 `选填`

| ID | 问题 | 影响范围 | 优先级 | 待查 |
| --- | --- | --- | --- | --- |
| D-{NNN} | {问题} | {范围} | {🟠/🟡} | {方法} |
