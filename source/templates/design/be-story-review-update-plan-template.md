# {STORY-ID} StoryReviewUpdatePlan

> **用途：** Story Review 完成缺陷挖掘与合理性判定后、修改 Story 主文档前生成。本计划记录 Story 与标准/DR/约束不一致的地方、存疑的地方、模糊的地方，以及每处应如何修改。Story Update 必须按本计划执行，禁止边改边补理由。

---

## 一、元信息

| 项 | 内容 |
| --- | --- |
| Story ID | {STORY-ID} |
| Review 轮次 | r{N} |
| 计划版本 | v{N} |
| 生成时间 | {YYYY-MM-DD HH:mm:ss} |
| 来源 Story | `{story-path}` |
| 来源 Supplement | `{story-supplement-path}` |
| Review 依据 | DR / Story 模板 / 约束 / 依赖 Story / 当前代码（如适用） |
| 计划状态 | Draft / Ready / Applied / Superseded |

---

## 二、本轮结论摘要

| 类型 | 数量 | 处理策略 |
| --- | ---: | --- |
| 确认缺陷 | 0 | 写入“更新计划”并执行 |
| 存疑项 | 0 | 标注需人工确认；未确认前不得强改 |
| 模糊项 | 0 | 补充可验证的文字、字段来源或边界 |
| 误报 | 0 | 只记录原因，不进入更新计划 |

---

## 三、问题清单

| 问题 ID | 来源检查项 | 问题类型 | 严重度 | 当前描述 | 证据 | 判定结果 | 归属 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SR-{N}-001 | A3-1 / C8-4 / D3-4 | 不一致 / 存疑 / 模糊 / 缺失 | 🔴 / 🟠 / 🟡 |  | DR § / Story § / 文件:行号 | ✅ 确认 / ⚠️ 待讨论 / ❌ 误报 | STORY-DRIFT / DR-DRIFT / DEPENDENCY-DRIFT / PRD-DRIFT / CONSTRAINT-DRIFT |

---

## 四、更新计划

> 只列需要修改 Story 主文档的项。误报不列入本表；待讨论项只有在“人工确认结果”明确后才能列入执行。

| 执行顺序 | 问题 ID | 目标章节 | 修改方式 | 修改内容摘要 | 验证方式 |
| ---: | --- | --- | --- | --- | --- |
| 1 | SR-{N}-001 | `## 数据模型` | 新增 / 修改 / 删除 / 替换 |  | 回扫目标章节 + 对照问题证据 |

---

## 五、字段链路与数据模型修订计划

> 当问题涉及入参、出参、DTO/VO、DO/PO、DB 字段、外部服务返回值或常量时必须填写。无相关变更时写“不涉及”并说明原因。
> 本节必须同时记录“字段链路明细表”和“横向流转对照图”需要如何改，避免只改文字不改链路图，或只改链路图不改字段明细。

| 字段/数据项 | 来源 | 入口层字段 | 应用层字段 | 领域层字段 | 持久化字段 | DB 列 / 外部字段 | 输出字段 | 本次处理 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | 用户输入 / CurrentUser / 路径参数 / 上游返回 / DB / 常量 / 配置 / 计算 | Request / Event | Command / DTO | DO / Entity | PO | table.column / SPI.field | VO / DTO / Event | 新增映射 / 修正类型 / 删除冗余 / 补来源 |

### 5.1 横向流转对照图修订

| 调整项 | 当前横向链路 | 目标横向链路 | 说明 |
| --- | --- | --- | --- |
|  | `source -> request -> dto -> do -> table.column` | `source -> request -> command -> do -> table.column` | 补充/修正/删除/标注不承接原因 |

```text
外部 HTTP / 上游事件      BFF/Controller 接收      Application/SPI DTO      Domain/DO            DB 列 / 外部字段
=============================================================================================================
{sourceField}       ->  {requestField}        ->  {commandField}        ->  {doField}       ->  {table.column}
{signature}         ->  {signature}           ->  —（签名验证用）        ->  —               ->  —
```

---

## 六、影响分析

| 影响对象 | 是否影响 | 说明 | 后续动作 |
| --- | --- | --- | --- |
| Story 主文档 | 是 / 否 |  | 按本计划更新 |
| Supplement | 是 / 否 |  | 标记问题状态和计划引用 |
| TestCase | 是 / 否 |  | 触发 TestCase Generate / 不触发 |
| Task 文档 | 是 / 否 |  | 触发 Task Generate / 不触发 |
| DR | 是 / 否 |  | 触发 DR Update / 仅记录 |
| 依赖 Story | 是 / 否 |  | 通知上游 / 下游 |
| 代码 | 是 / 否 |  | 进入 Coding / 不涉及 |

---

## 七、不修改边界

| 内容 | 不修改原因 |
| --- | --- |
|  | 误报 / 超出本 Story 范围 / 待人工确认 / 属于 DR 缺陷 |

---

## 八、执行后验收

| 验收项 | 通过标准 | 结果 |
| --- | --- | --- |
| 计划项全部落地 | “更新计划”每一行均能在 Story 主文档找到对应修改 | ☐ |
| 无计划外修改 | Story diff 不包含本计划未列出的业务语义变更 | ☐ |
| 字段链路闭环 | 有字段变更时，来源 → 入参/上下文 → 分层对象 → DB/外部依赖 → 出参完整，且明细表与横向流转对照图一致 | ☐ |
| Supplement 已回写状态 | 每个问题标记 Applied / Deferred / Superseded | ☐ |
| 需级联的产物已触发 | TestCase / Task / DR / 依赖 Story 按影响分析处理 | ☐ |
