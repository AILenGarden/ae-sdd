# STORY-<number>-<title>

> **本模板是 `be-story-template.md` 的升级版，集成"前端视角接口审视（①bis）"规范。**
>
> **适用场景：** 需要前端对接的 Story（绝大多数 Story）。
>
> **精简规则：** 纯后端无前端交互的 Story 可适当精简本模板的前端章节（标 🔴 的章节可标注"不涉及"），但 `①bis 6 维度审视` 仍推荐执行以保证接口契约的完整性。
>
> **与原 `be-story-template.md` 的差异：**
> 1. 新增 `🔴 前端接口契约` 章节（①bis 6 维度强制）
> 2. 新增 `🔴 状态流转前端展示` 章节
> 3. 新增 `🔴 前端错误码处理` 章节
> 4. 新增 `🔴 边界场景前端处理` 章节
> 5. 新增 `🔴 前端联调信息` 章节
> 6. 已有章节的表格中加前端列（涉及工程/触发入口/主流程/异常流程/非功能设计/测试策略/验收标准/用例设计/实现任务/依赖风险/未决问题）

---

## 元信息 `必填`

- 文档类型：Story 用户故事
- Story ID：
- 来源 PRD：
- 来源 DR：
- 功能点 ID：
- 优先级：P1 / P2 / P3
- 状态：Draft / Ready / In Progress / Done / Superseded
- **🔴 前端对接方（团队/Owner）：**
- **🔴 前端约束文件：** `constraints/frontend.md`（如有）
- **🔴 Mock 平台：** yapi / apifox / moco / 无 / 其他：____

> 示例：Story ID: STORY-003-BE，来源 PRD: PRD-001，来源 DR: DR-001-02，优先级: P1
> 前端对接方：@张三（Copilot前端组），前端约束：`constraints/frontend.md`，Mock：yapi（项目 ID: 123）

---

## 用户故事 `必填`

作为 `<角色>`，我希望 `<完成某个动作或达成某个目标>`，以便 `<获得明确业务价值>`。

> 示例：作为 `运营人员`，我希望 `修改用户账号状态`，以便 `快速停用违规账号或恢复正常账号`。

---

## 业务价值 `选填`

-

> 示例：运营人员可以实时响应违规行为，无需走审批流程即可停用问题账号。

---

## 范围 `必填`

### 包含

-

### 不包含

-

> 示例：
> 包含：修改单个用户状态（启用 / 停用）
> 不包含：批量修改状态、状态变更后的消息通知

---

## 前置条件 `选填（有特殊前置时填写）`

- 数据前置：
- 权限前置：
- 系统前置：
- **🔴 前端前置：**（如：需先上线前端页面 / 前端组件已就绪 / 前端权限已申请）

> 示例：
> 数据前置：目标用户已存在
> 权限前置：操作人具有 `user:status:edit` 权限
> 系统前置：无
> 前端前置：前端已申请 `user:status:edit` 前端权限并发布到联调环境

---

## 涉及工程 `必填`

| 工程 | 父工程 | 类型 | 变更说明 |
| ---- | ------ | ---- | -------- |
|      |        | BFF / SPI / Service / API |          |
|      |        | 🔴 **FE-Web / FE-MP / FE-MiniProgram** |  |

> 若工程不存在，需在父工程中创建子模块并注册到父 pom 的 modules 中。
>
> 🔴 前端工程必须明确（哪个端：Web/小程序/APP/Boss后台）；类型列填 `FE-Web` / `FE-MP` / `FE-MiniProgram`。
>
> 示例：
>
> | 工程                     | 父工程               | 类型    | 变更说明                                                     |
> | ------------------------ | -------------------- | ------- | ------------------------------------------------------------ |
> | icec-cloud-life-im-bff   | —                    | BFF     | 新增 `PATCH /user/{id}/profile` Controller 及参数校验，实现对外 API 接口 |
> | icec-cloud-life-im       | —                    | Service | 实现 AppService 编排、DomainService 校验、Repository 更新    |
> | icec-cloud-life-user-spi | icec-cloud-life-spi  | SPI     | 新增 `UpdateUserProfileRequest` / `UserDTO` 及 `UserService` 接口方法 |
> | icec-cloud-life-user-api | icec-cloud-life-api  | API     | 新增对外接口定义（APP 侧）                                   |
> | icec-cloud-boss-user-api | icec-cloud-boss-api  | API     | 新增对外接口定义（Boss 侧）                                  |
> | 🔴 icec-cloud-boss-web   | —                    | FE-Web  | Boss 后台新增资料编辑表单 + 昵称展示组件                    |

---

## 触发入口 `必填（填写适用项，不适用的留空）`

- REST 接口（`/api/{module}/v1/...`）：
- SPI 接口（服务间调用）：
- 消息事件（Kafka topic）：
- 后台任务（job-spring-boot-starter）：
- 外部系统：
- **🔴 前端入口（页面/按钮/事件）：**

> 示例：
> REST 接口：`PATCH /user/{id}/profile`
> SPI 接口：`UserService.updateUserProfile`
> 消息事件：无
> 后台任务：无
> 前端入口：Boss后台 → 用户管理 → 用户详情页 → "编辑资料"按钮（onClick）

---

## 主流程 `必填`

> 只描述正常路径，假设所有前置条件满足、所有校验通过。异常场景在下方"异常流程"中描述。

1.
2.
3.

> 示例：
> 1. 运营人员提交用户 ID 和目标状态
> 2. 校验用户是否存在
> 3. 校验目标状态与当前状态不同
> 4. 更新用户状态及 last_updated_date
> 5. 返回更新后的用户信息

---

## 实现方案决策基线 `必填`

> **目的：** 证明本 Story 的实现方案不是凭空设计。所有实现点都必须先做复用扫描，再参考成熟方案，最后用五维代码质量标准做取舍。
>
> **适用范围：** 接口、领域规则、状态机、应用编排、Repository/Mapper、通知、消息、定时任务、缓存、幂等、并发、外部集成、前端接口适配、测试支撑等所有实现点。

### 实现点清单

| # | 实现点 | 来源（DR/PRD/主流程） | 业务能力归属 | 是否高风险 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 1 |  |  |  | 是 / 否 |  |

### 现有能力复用扫描

| 实现点 | 搜索范围 | 搜索关键词 / 类名 / 接口名 | 已发现的现有能力 | 结论（复用/扩展/新建） | 不复用理由 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
|  | 项目资产 / 真实代码 / 依赖 Story / 历史 Task / 公共组件 |  |  |  |  | 文件路径:行号 / Story 章节 |

> **红线：** 不是只有第三方能力需要复用检查；所有实现点都必须检查。真实代码可访问时必须用代码搜索验证，不能只写“项目资产未提及”。

### 业内成熟方案参考

| 实现点 | 业内成熟方案 / 常见模式 | 适用条件 | 本 Story 是否采用 | 取舍理由 | 参考证据 |
| --- | --- | --- | --- | --- | --- |
| 状态机 | 事件驱动状态机 / Spring StateMachine / 枚举转移表 / 领域对象 transition | 多状态、多触发事件、多非法流转 | 是 / 否 |  | 官方文档 / 团队既有实现 / 项目代码 |

> 不要求盲目引入框架，但必须说明成熟方案是什么，以及本 Story 为什么采用或不采用。

### 五维代码质量评估

| 实现点 | 候选方案 | 可用性 | 高效性 | 可维护性 | 健壮性 | 可读性 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  | 复用现有能力 / 扩展现有能力 / 新建能力 |  |  |  |  |  |  |

> 五维标准：
> - **可用性**：满足业务场景、异常、边界和 AC。
> - **高效性**：避免重复查询、全表扫描、无谓远程调用、大事务和阻塞。
> - **可维护性**：复用团队既有抽象，能力单一归属，后续修改只改一处。
> - **健壮性**：覆盖失败、重试、幂等、并发、补偿、降级和可观测性。
> - **可读性**：命名、分层、流程和状态语义清楚。

### 核心能力归属与复用

| 业务能力 | 所属 Story / 模块 | 唯一实现点 | 允许调用方 | 复用方式 | 禁止重复实现位置 | 副作用 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  | 类/方法/领域服务/接口 |  | 直接调用 / 事件触发 / SPI |  | 状态变更 / 通知 / 统计 |

> 同一业务能力只能有一个唯一实现点；多个入口只能调用它，不能各写一套。接单、结单、分配、关闭、超时、通知、状态流转、幂等创建等高风险能力必须填写本表。

---

## 实现伪代码 `必填`

> 用**分层调用骨架**表达实现大方向：哪层调哪层、调用顺序、关键分支 / 事务边界 / 状态判定落在哪。目的是把握大方向，不是写完整实现。
> - 🔴 只写骨架，不写具体语法 / 完整实现（那是 Task 核心代码的事，本节不替代 Task）。
> - 🔴 **分层职责必须正确**（依据分层职责红线）：领域逻辑（状态流转/不变量/计算）写在 Domain；业务编排（调用顺序/事务边界/协调多依赖）写在 Application；Repository 只出现存取调用（find/save/update）。伪代码里若把业务判断写进 Repository，即暴露设计错误。
> - 标注每层职责注释，让读者一眼看清调用链是否分层合理。

```
// Interfaces 层：协议适配 + 参数校验
XxxController.method(req):
    @Valid 校验入参格式
    return xxxAppService.method(req 转 command)

// Application 层：业务编排 + 事务边界（不写领域规则）
@Transactional
XxxAppService.method(cmd):
    entity = xxxRepository.findById(cmd.id)        // 取
    if entity == null: throw XxxNotFoundException  // 异常路径
    entity.doDomainLogic(cmd.xxx)                  // 领域逻辑下沉到 Domain
    xxxRepository.save(entity)                     // 存
    publish(XxxEvent)（事务提交后触发）            // 事务外通知
    return XxxConverter.toDTO(entity)

// Domain 层：领域逻辑（状态流转规则、业务不变量、计算）
XxxEntity.doDomainLogic(xxx):
    if 不满足业务前置条件: throw XxxException
    if !canHandle(当前状态, 业务事件): throw IllegalTransitionException
    变更字段（充血模型自身完成）

// Infrastructure 层：Repository 只做存取（无任何业务判断）
XxxRepositoryImpl.save(entity):
    po = XxxConverter.toPO(entity)
    xxxMapper.updateById(po)   // 乐观锁 version
```

> 示例（业务事件驱动的状态机）：
>
> ```
> // Interfaces：参数校验 + 转发
> TicketController.claim(ticketId, request):
>     @Valid 校验请求格式
>     return ticketAppService.claim(ticketId, 当前操作人, request.remark)
>
> // Application：编排 + 事务边界
> @Transactional
> TicketAppService.claim(ticketId, operator, remark):
>     ticket = ticketRepository.findById(ticketId)          // 系统读取当前状态
>     if ticket == null: throw TicketNotFoundException       // → AC-002
>     ticket.handle(TicketEvent.CLAIM, operator, remark)     // 领域事件驱动状态机
>     ticketRepository.save(ticket)                          // 存
>     publish(TicketClaimedEvent)（提交后触发）               // 事务外
>     return TicketConverter.toDTO(ticket)
>
> // Domain：状态流转规则 + 不变量
> Ticket.handle(event, operator, remark):
>     if !canHandle(this.status, event): throw IllegalTransitionException  // → AC-003
>     根据业务事件计算下一状态并记录处理人/处理时间/原因
>
> // Infrastructure：Repository 只存取
> TicketRepositoryImpl.save(ticket):
>     ticketMapper.updateById(TicketConverter.toPO(ticket))  // 乐观锁 version
> ```

> **状态机红线：** 生命周期状态机不得把 `targetStatus` 作为主入口参数，不得公开 `updateStatus` / `changeStatus(targetStatus)` 驱动流转。调用方提交业务命令/事件，系统内部读取当前状态并决定下一状态。

---

## 异常流程 `必填`

| 场景 | 触发条件 | 系统行为 | 用户提示 / 日志 | AC ID | **🔴 前端处理建议** |
| --- | --- | --- | --- | --- | ---|
|  |  |  |  |  | Toast / 弹窗 / 跳转 / 静默 |

> 🔴 新增"前端处理建议"列：每个异常场景，前端如何展示（Toast/弹窗/跳转/静默）。
>
> 示例：
>
> | 场景 | 触发条件 | 系统行为 | 用户提示 / 日志 | AC ID | 🔴 前端处理建议 |
> | --- | --- | --- | --- | --- | ---|
> | 用户不存在 | userId 对应用户不存在 | 抛出业务异常 | "用户不存在" | AC-002 | 弹窗"用户不存在，请刷新列表"，关闭按钮返回 |
> | 状态未变更 | 目标状态与当前状态相同 | 抛出业务异常 | "用户状态未发生变化" | AC-003 | Toast 提示 2 秒后消失 |

### 异常说明 `选填`

> 当异常场景需要详细说明时使用，如异常的业务原因、系统行为、日志要求、告警要求等。

| 异常场景 | 说明 |
| --- | --- |
|  |  |

> 示例：
>
> ### 异常说明
>
> **用户不存在异常：**
>
> - **触发原因**：目标用户被删除或从未创建。
> - **系统行为**：抛出业务异常，事务回滚。
> - **日志要求**：记录 error 日志，包含 userId。
>
> | 异常场景 | 说明 |
> | --- | --- |
> | 用户不存在 | 见上方详细说明 |

---

## 接口契约 `必填`

### REST 接口（BFF 层）

**接口：** `{HTTP Method} /{resource}`

**接口说明：** `{必填。描述本接口的：①数据变化（哪些字段从什么值变成什么值）；②业务逻辑（校验什么、触发什么）。禁止只写功能名称，必须写清楚数据流向和业务规则。格式：【数据变化】+【业务逻辑】}`

**应用场景：** `{描述本接口的典型使用场景，例如：Copilot 检测到用户回复时触发 / Agent 管理后台人工操作}`

**调用方须知：** `{描述调用方需要知道的事项，包括：①调用方式（同步/异步）；②超时建议；③失败重试策略；④幂等性说明；⑤并发注意事项；⑥结果判断方式（如何判断成功/失败）。如无需特别说明，填"无特殊要求"。}`

> **⚠️ 字段类型强制规范（禁止违反）：**
> - **ID 字段**：必须使用 `Long` 或 `Integer`，禁止使用 `String`
> - **金额字段**：必须使用 `BigDecimal`，禁止使用 `Double` / `Float`
> - **布尔字段**：必须使用 `Boolean`，禁止使用 `String`
> - **枚举字段**：必须使用 `String`，禁止使用 `Integer`
> - **时间字段**：必须使用 `LocalDateTime` / `LocalDate`，禁止使用 `String`

> **🔴 出入参四维强制规范：每个字段都必须填写四维，禁止留白。**
> - **来源**：入参=调用方从哪里取得这个值（路径参数/上游接口返回/用户输入/CurrentUser/常量配置）；出参=服务端这个值从哪里产生（查 DB 哪张表哪个字段/计算/常量/透传上游）。🔴 来源缺失（凭空出现的值）= 阻断型。
> - **必要性**：业务上为什么必须有这个字段（禁止"方便扩展""以后可能用"）。
> - **合理性**：类型/命名/结构是否最优。
> - **可行性**：该值在标注的来源处确实拿得到、技术上能支撑。

**Request**

| 字段 | 类型 | 必填 | 校验规则 | 说明 | 来源 | 必要性 | 合理性 | 可行性 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  |  | 是 / 否 |  |  | 调用方从哪取得（如：路径参数/上游XxxApi返回） | 业务上为何必须有 | 类型/结构是否最优 | 该来源处确实可得 |

**VO**

| 字段 | 类型 | 说明 | 来源 | 必要性 | 合理性 | 可行性 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  | 服务端从哪产生（如：查 user 表 status 字段/计算） | 调用方为何需要 | 类型/结构是否最优 | 该值确实能产出 |

> 示例：
> 接口：`PATCH /user/{id}/profile`
>
> 接口说明：`【数据变化】user.nickname: 原昵称 → 新昵称；【业务逻辑】校验用户存在 → 校验昵称格式 → 更新用户资料 → 发送 UserProfileChangedEvent 事件`
>
> 应用场景：`Boss 管理后台 → 用户管理 → 点击"编辑资料"按钮`
>
> 调用方须知：`①同步调用，超时建议 3s；②重复提交使用 requestVersion 做并发控制；③并发：多线程同时修改时，后到的请求可能因版本变化而失败`
>
> Request
>
> | 字段 | 类型 | 必填 | 校验规则 | 说明 | 来源 | 必要性 | 合理性 | 可行性 |
> | --- | --- | --- | --- | --- | --- | --- | --- | --- |
> | id | Long | 是 | > 0 | 用户 ID（路径参数） | 调用方从 URL 路径参数传入（管理后台列表行的 userId） | 用于服务端定位目标用户记录 | Long 符合 ID 类型规范 | 列表接口已返回 userId，可得 |
> | nickname | String | 是 | 长度 1-64，trim 后不可为空 | 新昵称 | 用户在资料编辑表单输入 | 用于服务端更新展示昵称 | String 合规，长度受控 | 前端表单可取得 |
>
> VO
>
> | 字段 | 类型 | 说明 | 来源 | 必要性 | 合理性 | 可行性 |
> | --- | --- | --- | --- | --- | --- | --- |
> | id | Long | 用户 ID | 查 user 表主键 | 调用方关联查询/日志记录 | Long 合规 | 更新后必有主键 |
> | nickname | String | 更新后昵称 | 查 user.nickname 更新后值 | 调用方需展示最新资料 | String 合规 | 更新后可读取 |
> | lastUpdatedDate | LocalDateTime | 最后更新时间 | 查 user.last_updated_date | 调用方用于展示/校验 | 时间用 LocalDateTime 合规 | 审计字段自动填充 |

---

### SPI 接口（Service 层）

**接口：** `XxxDTO methodName(XxxRequest request)`

**接口说明：** `{必填。描述本接口的：①数据变化（哪些字段从什么值变成什么值）；②业务逻辑（校验什么、触发什么）。禁止只写功能名称，必须写清楚数据流向和业务规则。格式：【数据变化】+【业务逻辑】}`

**应用场景：** `{描述本接口的典型使用场景和调用方，例如：被 CopilotStory 调用，当用户回复时触发状态更新 / 被 TicketStateAppService 调用，状态流转后同步更新会话状态}`

**调用方须知：** `{描述调用方需要知道的事项，包括：①调用方式（同步/异步）；②超时建议；③失败重试策略；④幂等性说明；⑤并发注意事项；⑥结果判断方式（如何判断成功/失败）。如无需特别说明，填"无特殊要求"。}`

> **⚠️ 字段类型强制规范（禁止违反）：**
> - **ID 字段**：必须使用 `Long` 或 `Integer`，禁止使用 `String`
> - **金额字段**：必须使用 `BigDecimal`，禁止使用 `Double` / `Float`
> - **布尔字段**：必须使用 `Boolean`，禁止使用 `String`
> - **枚举字段**：必须使用 `String`，禁止使用 `Integer`
> - **时间字段**：必须使用 `LocalDateTime` / `LocalDate`，禁止使用 `String`

> **🔴 出入参四维强制规范：每个字段都必须填写四维（来源/必要性/合理性/可行性），禁止留白。来源缺失（凭空出现的值）= 阻断型。**（含义同 REST 接口段说明）

**Request**

| 字段 | 类型 | 必填 | 说明 | 来源 | 必要性 | 合理性 | 可行性 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  | 是 / 否 |  | 调用方从哪取得 | 业务上为何必须有 | 类型/结构是否最优 | 该来源处确实可得 |

**DTO**

| 字段 | 类型 | 说明 | 来源 | 必要性 | 合理性 | 可行性 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  | 服务端从哪产生 | 调用方为何需要 | 类型/结构是否最优 | 该值确实能产出 |

> 示例：
> 接口：`UserDTO updateUserProfile(UpdateUserProfileRequest request)`
>
> 接口说明：`【数据变化】User.nickname: {原昵称} → {新昵称}；【业务逻辑】校验 userId 对应用户存在 → 校验昵称格式 → 更新资料 → 返回最新用户信息`
>
> 应用场景：`被 Boss 用户 BFF 调用，当运营人员提交资料编辑表单时触发`
>
> 调用方须知：`①同步调用，超时建议 2s；②create 接口：相同 conversationId 的重复请求返回幂等结果；transition 接口：相同 ticketId + expectedVersion 可重试；③失败重试前需确认上一次请求是否已落库`
>
> Request
>
> | 字段 | 类型 | 必填 | 说明 | 来源 | 必要性 | 合理性 | 可行性 |
> | --- | --- | --- | --- | --- | --- | --- | --- |
> | userId | Long | 是 | 用户 ID | 调用方 CopilotStory 上下文持有的 userId | 用于服务端定位目标用户记录 | Long 符合 ID 规范 | 上游上下文已含 userId |
> | nickname | String | 是 | 新昵称 | 调用方从资料编辑表单取得 | 用于服务端更新展示昵称 | String 合规，长度受控 | 上游可确定取值 |
>
> DTO
>
> | 字段 | 类型 | 说明 | 来源 | 必要性 | 合理性 | 可行性 |
> | --- | --- | --- | --- | --- | --- | --- |
> | id | Long | 用户 ID | 查 user 表主键 | 调用方关联查询/日志记录 | Long 合规 | 更新后必有主键 |
> | nickname | String | 更新后昵称 | 查 user.nickname 更新后值 | 调用方需展示最新资料 | String 合规 | 更新后可读取 |
> | lastUpdatedDate | LocalDateTime | 最后更新时间 | 查 user.last_updated_date | 调用方用于展示/校验 | 时间用 LocalDateTime 合规 | 审计字段自动填充 |

---

## 🔴 前端接口契约 `必填（①bis 6 维度强制）`

> **本章节是 ①bis 强制要求，禁止缺失。** 接口定义不止给后端实现看，前端也要看、也要能根据 Story 直接对接联调。每个 Story 涉及的所有接口都必须在本章节列出，前端拿到本章节即可直接定义 TypeScript Interface / Props / Form Schema。

### 接口清单

| # | 接口 | Method | URL | 调用方（页面/组件） | 调用时机 | 串/并行 | 失败降级 |
|---|------|--------|-----|------------------|---------|--------|---------|
| 1 |  |  |  |  | 页面加载/按钮点击/表单提交/定时刷新 | 串行/并行/独立 | 提示重试/部分失败/全部失败 |
| 2 |  |  |  |  |  |  |  |

> 🔴 每个字段都不能空。

### 接口 1：`<接口名>`

**请求：**
​```json
{METHOD} {URL}
Headers: Content-Type: application/json, Authorization: Bearer {token}, {其他}

{
  "field1": "value1",    // 字段含义（前端UI用途），类型，是否必填，默认值，取值范围
  "field2": 123,         // 字段含义，类型，是否必填，示例值
  "field3": ["a", "b"]   // 字段含义，类型，可选，示例值
}
​```

**响应：**
​```json
HTTP {status}
{
  "code": 0,
  "data": {
    "id": 123456789012345,                  // 字段含义，Long 雪花ID
    "status": "WAITING",                     // 字段含义，String 枚举：WAITING/IN_PROGRESS/RESOLVED/UNRESOLVED/TIMEOUT_CLOSED
    "createTime": "2026-06-03 10:30:00",    // 字段含义，格式 yyyy-MM-dd HH:mm:ss，时区 UTC+8
    "amount": 9999,                          // 字段含义，单位：分（前端展示需 /100），Integer
    "tags": ["支付", "退款"]                  // 字段含义，String[]，前端 chip 展示
  }
}
​```

**字段类型约定（🔴 必填，禁止违反）：**
- **ID 字段**：Long（雪花）/ String（UUID）/ Integer（自增），在响应示例中**必须显式标注**类型
- **金额字段**：**单位**（分/元）+ **精度**，禁止只给数字不标单位
- **时间字段**：**格式**（ISO8601 / `yyyy-MM-dd HH:mm:ss` / 时间戳）+ **时区**（UTC+8 / UTC）
- **枚举字段**：列出全部枚举值 + 每个值的含义
- **布尔字段**：true/false 对应的业务含义

**调用流程说明（如适用）：**
- 触发时机：页面加载时 / 按钮点击时 / 表单提交时
- 触发顺序：本接口 → 下游接口 X（串行） / 与接口 Y 并行
- 防抖/节流：搜索联想 300ms 防抖
- 取消：页面跳转时 abort

### 状态展示建议（如本接口涉及状态字段，🔴 必填）

| 状态值 | 含义 | UI 颜色 | UI 图标 | UI 文案 | 按钮可点击 | 终态表现 | 实时性 |
|--------|------|---------|---------|---------|-----------|---------|--------|
| WAITING | 待接单 | 橙色 `#FFA500` | 时钟 ⏰ | "待接单" | 接单、转派 | — | 轮询 5s |
| IN_PROGRESS | 处理中 | 蓝色 `#1890FF` | loading 🔄 | "处理中" | 结单、转派 | — | 轮询 5s |
| RESOLVED | 已解决 | 绿色 `#52C41A` | 对勾 ✅ | "已解决" | 关闭 | 按钮置灰 | 轮询 5s |
| UNRESOLVED | 未解决 | 红色 `#FF4D4F` | 叉 ❌ | "未解决" | 重开 | 按钮置灰 | 轮询 5s |
| TIMEOUT_CLOSED | 超时关闭 | 灰色 `#999999` | 锁 🔒 | "超时关闭" | 重开 | 按钮置灰 | 轮询 5s |

> 🔴 颜色给具体色值（不要"绿色"这种模糊描述），图标给具体 emoji 或 iconfont 名称。
> 🔴 实时性必须明确：轮询 N 秒 / WebSocket / SSE / 手动刷新。

### 边界与异常处理（如有边界场景，🔴 必填）

| 场景 | 前端处理 |
|------|---------|
| 空数据 | 空态：插画 + "暂无数据" + 重新加载按钮 |
| 超长字段 | 截断到 50 字符 + ...，hover tooltip 显示完整 |
| 并发冲突 | 乐观锁失败 → 弹窗"数据已被他人修改，请刷新" + 刷新按钮 |
| 重复提交 | 提交按钮置灰 3 秒（防抖）+ Loading 状态 |
| 弱网/超时 | Loading 超 10 秒自动取消 + 失败重试按钮 |
| 大数据量 | 骨架屏 + 虚拟滚动（>100 行） |
| 离线 | Toast 提示"网络已断开，请检查后重试" + 禁用操作按钮 |

### 联调信息（如有联调需求，🔴 必填）

| 联调项 | 内容 |
|--------|------|
| Mock 平台 | yapi（项目 ID: 123，URL: https://yapi.example.com/project/123） |
| 测试环境 | https://test-api.example.com |
| 测试账号 | test_user / Test@123 |
| 联调时间窗口 | 2026-06-10 ~ 2026-06-15 |
| 前端联系人 | @张三 |
| 后端联系人 | @李四 |
| 兼容性 | 需并行支持老接口 3 天（灰度切流） |

### ①bis 6 维度自检（🔴 全部通过才能视为本章节完成）

- [ ] **维度 1：接口契约完整性** —— 字段类型/必填/示例/含义/时间格式/时区/金额单位/枚举含义全部明确
- [ ] **维度 2：调用流程与依赖** —— 调用时机/串并行/失败降级/防抖/取消已说明
- [ ] **维度 3：状态流转前端展示** —— 状态枚举 + 颜色 + 图标 + 文案 + 实时性齐备
- [ ] **维度 4：错误码前端处理** —— 业务/校验/鉴权/限流/系统 5 类错误码前端处理建议已给出（详见下方"前端错误码处理"章节）
- [ ] **维度 5：边界与异常场景** —— 空数据/超长/并发/重复提交/弱网/大数据/上传/离线/i18n 已覆盖
- [ ] **维度 6：联调支持** —— mock / 环境 / 时间窗口 / 联系人 / 兼容性齐备

---

## 错误码 `必填（有业务错误时）`

| 错误码 | 错误信息 | 触发场景 |
| --- | --- | --- |
|  |  |  |

> 示例：
>
> | 错误码 | 错误信息 | 触发场景 |
> | --- | --- | --- |
> | 11001 | 用户不存在 | userId 对应用户不存在 |
> | 11002 | 用户状态未发生变化 | 目标状态与当前状态相同 |

---

## 🔴 前端错误码处理 `必填（有业务错误时）`

> 与上方"错误码"章节配套：上方定义**后端错误码**，本章节定义**前端如何处理这些错误码**。

| 错误码 | HTTP 状态码 | 触发条件 | 🔴 前端处理方式 | 🔴 UI 文案建议 | 🔴 是否需要重试 |
|--------|------------|---------|---------------|--------------|----------------|
| 11001 | 400 | 用户不存在 | 弹窗 | "用户不存在，请刷新列表" | 否 |
| 11002 | 400 | 状态未变更 | Toast（2秒） | "用户状态未发生变化" | 否 |
| 21041 | 400 | 会话不存在 | 弹窗 + 跳转登录 | "会话已失效，请重新登录" | 否 |
| 21042 | 400 | 重复创建 | 弹窗 + 跳转详情 | "工单已存在" | 否 |
| 401 | 401 | 未登录 | 跳转登录页 | "请先登录" | 否 |
| 403 | 403 | 无权限 | 弹窗 | "您没有操作权限" | 否 |
| 429 | 429 | 限流 | Toast + 倒计时 | "操作太频繁，请 N 秒后重试" | 是（倒计时结束） |
| 500 | 500 | 系统异常 | Toast | "系统繁忙，请稍后重试" | 是（用户手动） |

> 🔴 每一行 5 列都不能空：HTTP 状态码、触发条件、处理方式、UI 文案、是否重试。
> 🔴 处理方式枚举：Toast（短暂提示）/ 弹窗（需确认）/ 跳转 / 静默（仅日志）。

---

## 🔴 状态流转前端展示 `必填（涉及状态流转时）`

> 即使接口契约章节里已列状态展示建议，本章节作为**总览/索引**，供前端快速查阅。
> 如果 Story 不涉及状态字段，本章节可标注"不涉及"。

| 状态值 | 含义 | UI 颜色 | UI 图标 | UI 文案 | 按钮可点击 | 终态 | 实时同步方式 |
|--------|------|---------|---------|---------|-----------|------|------------|
|  |  |  |  |  |  |  | 轮询 N 秒 / WebSocket / 手动 |

> 🔴 同接口契约章节要求：颜色给具体色值，图标给具体名称，实时性给具体方式。

---

## 🔴 边界场景前端处理 `必填（涉及边界场景时）`

> 如果 Story 不涉及复杂边界场景，本章节可标注"不涉及"。

| 场景 | 触发条件 | 前端处理 | UI 表现 |
|------|---------|---------|---------|
| 空数据 | 接口返回空数组/空对象 | 显示空态 | 插画 + "暂无数据" + "重新加载" 按钮 |
| 超长字段 | 字段值超过展示阈值 | 截断 + tooltip | "前 50 字符..." + hover 显示完整 |
| 并发冲突 | 乐观锁失败 | 提示刷新 | 弹窗"数据已被他人修改" + "刷新" 按钮 |
| 重复提交 | 用户短时间内多次点击 | 防抖/节流 | 提交按钮置灰 + Loading |
| 弱网/超时 | 网络慢/超时 | Loading 状态 | 超过 10s 提示"网络较慢，请稍候" |
| 加载失败 | 接口 5xx | 重试 | Toast + 失败重试按钮 |
| 离线 | 无网络 | 提示 + 禁用 | Toast"网络已断开" + 按钮置灰 |
| 文件过大 | 上传文件 >10MB | 拦截 + 提示 | Toast"文件过大，请压缩后上传" |
| 国际化 | 多语言 | 走 i18n 资源 | 文案从语言包读取，key: `ticket.status.WAITING` |

---

## 数据模型 `选填（有数据库变更或字段链路需要说明时填写）`

> 表操作须遵守：软删除（delete_flag）、更新记录必须同时更新 last_updated_date。
> 若无数据库变更但存在接口字段、DTO/VO 字段、外部依赖字段或读写链路，也应至少填写“字段链路映射”。

### 表变更

| 表名 | 变更类型 | 说明 |
| --- | --- | --- |
|  | 新增 / 修改 |  |

### 字段变更

**`{表名}`**

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
|  |  | NOT NULL / DEFAULT ... |  |

### 索引变更

| 表名 | 索引名 | 类型 | 字段 | 说明 |
| --- | --- | --- | --- | --- |
|  |  | 普通 / 唯一 |  |  |

### DDL

```sql
-- 在此粘贴建表或变更 SQL
```

### DB 操作（CRUD）逻辑 `必填（有数据库读写时）`

> 🔴 不只列"对哪张表做了什么操作"，必须写清每个 CRUD 操作的**运行时逻辑**：触发条件、WHERE 定位、字段变更（从→到）、幂等键。Coding 直接照此写 SQL/Repository。仓储只做存取，业务判断写在 Domain/Application（见分层职责红线）。

| # | 操作 | 目标表 | 触发条件 | WHERE/筛选条件 | 字段变更（从→到） | 幂等键/唯一约束 | 事务范围 | 并发控制 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | INSERT |  | 何种业务动作触发 | —（插入无 WHERE） | 写入哪些字段及值来源 | 防重复的唯一键 | 所属事务 | — |
| 2 | UPDATE |  | 何种条件下更新 | 按什么精确定位（避免多行） | 字段 从X→Y | 乐观锁 version / 状态前置 | 所属事务 | 乐观锁/悲观锁 |
| 3 | DELETE |  | 何种条件下删除 | 按什么定位 | is_deleted 0→1（优先逻辑删除） |  | 所属事务 | — |
| 4 | SELECT |  | 何处需要查询 | 查询条件（是否走索引） | —（读不变更） | — | —（只读） | — |

> 示例：
>
> | # | 操作 | 目标表 | 触发条件 | WHERE/筛选条件 | 字段变更（从→到） | 幂等键/唯一约束 | 事务范围 | 并发控制 |
> | --- | --- | --- | --- | --- | --- | --- | --- | --- |
> | 1 | UPDATE | boss_user | 收到冻结请求且校验通过 | `id = #{userId} AND status = 'ACTIVE'` | status: ACTIVE→INACTIVE；login_enabled: true→false | 状态前置条件（仅 ACTIVE 可冻结）+ version 乐观锁 | updateUserStatus() @Transactional | 乐观锁 version |
> | 2 | SELECT | boss_user | 更新前查当前状态 | `id = #{userId}`（走主键） | — | — | 同上事务内 | — |

### 字段链路映射 `必填（有接口字段、DTO/VO 字段或数据库读写时）`

> 🔴 本节把“字段从哪里来、经过哪些对象、落到哪里、返回给谁”一次写清。凡涉及 REST/SPI/事件入参、返回值、DTO/VO、DO/PO、DB 字段、外部服务返回值或常量/配置的 Story，都必须填写。没有数据库变更但有读写或字段透传时也要填写。
>
> **链路必须闭环：** 来源 → 入口层字段 → 应用层字段 → 领域层字段 → 持久化字段 → DB 列 / 外部字段 → 输出字段。某层不涉及时写“不涉及”并说明原因，禁止留空。
>
> **推荐双格式：**
> - **字段链路明细表**：用于 Review 逐字段核对。
> - **横向流转对照图**：用于人工审核快速看懂字段从入口到落库/出参的完整链路。

| 字段/数据项 | 来源 | 入口层字段 | 应用层字段 | 领域层字段 | 持久化字段 | DB 列 / 外部字段 | 输出字段 | 转换/校验规则 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | 用户输入 / CurrentUser / 路径参数 / 上游返回 / DB / 常量 / 配置 / 计算 | Request / Event | Command / DTO | DO / Entity | PO | table.column / SPI.field | VO / DTO / Event | 类型转换、枚举映射、默认值、脱敏、格式化 |

> 示例：
>
> | 字段/数据项 | 来源 | 入口层字段 | 应用层字段 | 领域层字段 | 持久化字段 | DB 列 / 外部字段 | 输出字段 | 转换/校验规则 |
> | --- | --- | --- | --- | --- | --- | --- | --- | --- |
> | userId | 路径参数 `{userId}` | `UpdateUserProfileRequest.userId` | `UpdateUserProfileCommand.userId` | `BossUserDO.id` | `BossUserPO.id` | `boss_user.id` | `UpdateUserProfileVO.userId` | ID 用 Long；必须存在 |
> | nickname | 用户输入 | `nickname` | `nickname` | `BossUserDO.nickname` | `BossUserPO.nickname` | `boss_user.nickname` | `nickname` | trim 后长度 1-64；禁止空白字符串 |
> | operatorId | CurrentUser | 不由前端传 | `operatorId` | 审计字段 | `last_updated_by` | `boss_user.last_updated_by` | 不返回 | 从登录上下文获取，禁止前端传 |

#### 横向流转对照图 `推荐`

> 横向图的列必须按本 Story 的真实调用链命名，不要求固定为下面示例。常见列：外部 HTTP / BFF 接收 / Controller Request / Application Command / SPI DTO / Domain DO / Persistence PO / DB 列 / Response VO。
>
> 规则：
> - 每行一个来源字段或派生字段。
> - 字段跨层改名时用 `->` 串起来。
> - 某层不承接时写 `—`，并在括号中说明原因，如 `—（签名验证用）`、`—（路由用，不落库）`、`—（丢弃）`。
> - DB 唯一键、索引、签名校验、枚举转换、时间单位转换等必须在对应列标注。

```text
外部 HTTP            BFF/Controller 接收      Application/SPI DTO      Domain/DO            DB 列 / 外部字段
=============================================================================================================
{sourceFieldA}   ->  {requestFieldA}      ->  {commandFieldA}       ->  {doFieldA}       ->  {table.column_a}
{sourceFieldB}   ->  {requestFieldB}      ->  {commandFieldB}       ->  —（路由用）       ->  —
{signature}      ->  {signature}          ->  —（签名验证用）        ->  —                ->  —
{eventId}        ->  {eventId}            ->  {sourceEventId}       ->  {sourceEventId}  ->  {source_event_id} (UNIQUE)
```

### 常量与魔法值

> 当 Story 引用 DR 中的常量值（如默认值、阈值上限）或 AC 中出现硬编码数值时，必须填写本节，说明值的来源和配置入口。读者不应去 DR 文档中翻找这些值的出处。

| 常量/魔法值 | 值 | 来源（DR 章节/其他 Story） | 配置入口 | 说明 |
|------------|-----|---------------------------|---------|------|
| | | | | |

> 示例：
>
> | 常量/魔法值 | 值 | 来源 | 配置入口 | 说明 |
> |------------|-----|------|---------|------|
> | MAX_CONCURRENCY_DEFAULT | 5 | DR 第7节 cs_user.max_concurrency | STORY-004-BE（坐席在线状态管理） | 最大并发接待数默认值 |
> | SESSION_TIMEOUT_MINUTES | 30 | DR 第9节 BR-14 | 无（固定值） | 会话超时时间 |

> 示例：
>
> 表变更
>
> | 表名 | 变更类型 | 说明 |
> | --- | --- | --- |
> | boss_user | 修改 | 新增 status 字段 |
>
> 字段变更 `boss_user`
>
> | 字段名 | 类型 | 约束 | 说明 |
> | --- | --- | --- | --- |
> | status | varchar(20) | NOT NULL DEFAULT 'ACTIVE' | 用户状态：ACTIVE / INACTIVE |
>
> 索引变更：无
>
> DDL
>
> ```sql
> ALTER TABLE boss_user
>     ADD COLUMN status VARCHAR(20) NOT NULL DEFAULT 'ACTIVE' COMMENT '用户状态：ACTIVE / INACTIVE';
>
> CREATE INDEX idx_boss_user_status ON boss_user (status);
> ```

---

## 配置变更 `选填（有配置新增或修改时填写）`

### bootstrap.yml / application.yml

| 配置项 | 所属工程 | 变更类型 | 说明 |
| --- | --- | --- | --- |
|  |  | 新增 / 修改 |  |

### Nacos 配置

| 配置项 | DataId | 变更类型 | 说明 |
| --- | --- | --- | --- |
|  |  | 新增 / 修改 |  |

> 示例：
>
> bootstrap.yml / application.yml
>
> | 配置项 | 所属工程 | 变更类型 | 说明 |
> | --- | --- | --- | --- |
> | feign.client.user.url | icec-cloud-life-im-bff | 新增 | user 服务 Feign 地址 |
>
> Nacos 配置
>
> | 配置项 | DataId | 变更类型 | 说明 |
> | --- | --- | --- | --- |
> | user.status.cache.ttl | icec-cloud-life-im.yaml | 新增 | 用户状态缓存 TTL，默认 300s |

---

## 非功能设计 `必填（有写操作/外部依赖/失败场景时）`

> 承载 Story Review 阶段 C/E 的检查点。纯只读、无外部依赖、无副作用的能力可在对应小节标注"不涉及"。

### 幂等设计（写操作/有副作用的接口必填）

| 接口/操作 | 幂等键 | 重复请求行为 | 并发同键处理 | 兜底机制 |
| --- | --- | --- | --- | --- |
|  | 业务唯一键 | 返回上次结果/明确错误码/拒绝 | 唯一约束/乐观锁/分布式锁 | 唯一索引/前置状态校验 |

> 示例：
>
> | 接口/操作 | 幂等键 | 重复请求行为 | 并发同键处理 | 兜底机制 |
> | --- | --- | --- | --- | --- |
> | 更新用户资料 | userId + requestVersion | 相同版本重复提交返回上次结果或明确并发错误 | 乐观锁 version | `WHERE id=#{userId} AND version=#{requestVersion}` |

### 失败与补偿

| 失败点 | 失败类型 | 处理策略 | 最终一致性保障 |
| --- | --- | --- | --- |
|  | 事务内/事务外 | 回滚/重试N次/记录待补偿/告警 | 补偿任务/对账/死信队列 |

> 示例：
>
> | 失败点 | 失败类型 | 处理策略 | 最终一致性保障 |
> | --- | --- | --- | --- |
> | 发送 UserStatusChangedEvent | 事务外 | 重试 3 次，仍失败记录待补偿 | 定时补偿任务扫描未发送事件重发 |

### 可观测性

| 关键动作 | 指标/日志 | 业务标识 | 失败告警 |
| --- | --- | --- | --- |
|  | 成功失败计数/耗时 | traceId + 业务键 | 是/否 |

### 可回滚与灰度 `选填（改变线上行为/依赖外部/有数据迁移时必填）`

| 风险点 | 回滚方式 | 开关/降级 | 数据可逆性 |
| --- | --- | --- | --- |
|  | 开关关闭/降级旧逻辑 | 功能开关名 | 迁移脚本可逆/新增字段可空 |

### 🔴 前端性能与体验 `选填`

| 场景 | 性能指标 | 体验要求 |
|------|---------|---------|
| 首屏加载 | FCP < 1.5s / LCP < 2.5s | 骨架屏 + 渐进式渲染 |
| 接口响应 | TTFB < 200ms | Loading 状态展示 |
| 列表滚动 | 60fps | 虚拟滚动（>100 行） |
| 表单提交 | 响应 < 1s | 提交按钮立即置灰 + Loading |
| 弱网 | 3G 网络下可用 | 资源压缩 + 缓存策略 |
| 缓存 | 重复访问 < 100ms | localStorage / sessionStorage 缓存列表数据 |

---

## 测试策略 `选填（有复杂测试场景时填写）`

### 测试分层

| 层级 | 测试对象 | 测试策略 | 外部依赖处理 |
|------|---------|---------|-------------|
| L1 接口 | Controller / Facade | 🔴 能用真实 HTTP 请求测的，必须走真实 HTTP（SpringBootTest RANDOM_PORT + TestRestTemplate），不退化成方法调用或 MockMvc | Mock 签名验证、Mock 第三方服务 |
| L2 应用 | AppService | 直接方法调用，Mock 仓储/领域服务 | Mock Repository、Mock 外部 Client |
| L3 领域 | DomainService / 聚合根 | 纯单元测试 | Mock 一切外部依赖 |
| L4 基础设施 | Repository / Filter | 集成测试或 Mock | Mock DB Connection |
| **🔴 L5 前端组件** | **Vue/React 组件** | **Vitest + Vue Test Utils / React Testing Library，组件级单元测试** | **Mock API Client** |
| **🔴 L6 前端 E2E** | **完整业务流程** | **Playwright / Cypress，端到端测试** | **走真实后端 + mock 外部服务** |

### 测试原则

1. **能走真实链路就不用模拟**：能走真实 HTTP 的接口走真实 HTTP，核心落库走真实 DB，不用 MockMvc/全 Mock 替代
2. **由外而内**：先验证接口层行为，再验证业务逻辑
3. **分层 Mock**：每层只 Mock 直接依赖，不跨层 Mock
4. **外部依赖隔离**：签名校验、外部 API 调用等使用 Stub 或 Mock
5. **异常优先**：先测试异常路径，再测试正常路径
6. **🔴 前端组件独立测试**：组件 props/emit/slot/事件 单独测试，与后端测试解耦

### 常见问题处理

| 问题 | 解决方案 |
|------|---------|
| 真实 HTTP 测试序列化失败 | 🔴 修复序列化配置（Jackson 配置/字段注解），不得退回方法调用或 MockMvc 绕过——序列化失败正是真实 HTTP 要暴露的问题 |
| 签名验证复杂 | Filter 层可独立单元测试，但接口主流程仍需走真实 HTTP 端到端验证一次 |
| 第三方 API 调用 | 使用 WireMock 或 Mock Server |
| 数据库依赖 | 使用 H2 内存数据库或 Mock MyBatis-Plus Mapper |
| **🔴 前端组件依赖复杂** | **使用 provide/inject 或 Pinia/Vuex 注入 mock 数据，避免组件直接 import 真实 API** |
| **🔴 前端 E2E 不稳定** | **使用 data-testid 属性定位元素，避免依赖文案/样式** |

### 测试层级选择决策

| 场景 | 推荐测试层级 | 说明 |
|------|------------|------|
| 纯算法/领域逻辑（如签名校验、状态机） | 单元测试 | 直接实例化测试，无外部依赖 |
| 应用层编排（如 AppService 调用 Repository） | 单元测试 + Mock | Mock 直接依赖，保留业务逻辑验证 |
| Controller 接口（参数校验、返回值封装） | 接口测试 | 🔴 真实 HTTP（SpringBootTest RANDOM_PORT + TestRestTemplate），MockMvc 仅框架过老时降级 |
| 涉及真实外部系统（融云、极光） | 联调测试/集成测试 | 单元测试阶段标记为 ⏸️ Skip |
| 涉及真实数据库事务 | 集成测试 | 单元测试阶段标记为 ⏸️ Skip |
| **🔴 纯展示组件** | **L5 组件测试** | **Vitest 单元测试 props/emit 即可** |
| **🔴 含交互的容器组件** | **L5 组件测试 + L6 E2E** | **组件级测交互，E2E 测全链路** |
| **🔴 状态展示组件** | **L5 组件测试** | **每个状态值一个 snapshot/用例** |
| **🔴 涉及真实后端流程** | **L6 E2E** | **Playwright 端到端，验证联调结果** |

### 不可单元测试的识别

以下情况不适合单元测试，应标记为联调/集成测试：
- 需要真实外部系统（融云 Webhook、极光推送等）
- 需要真实数据库事务和回滚
- 需要完整 HTTP 链路验证
- 需要网络通信时序（如超时、重试）

**处理方式：** 在验收标准的"测试实现"列标注 `⏸️ {原因}`，后续批次补测。

### 测试实现记录

| 测试类 | 层级 | 测试数 | 状态 |
|--------|------|--------|------|
| | | | ⬜ Pending |
| **🔴 {前端组件}.test.ts** | **L5** | | ⬜ Pending |
| **🔴 {E2E 测试}.spec.ts** | **L6** | | ⬜ Pending |

---

## 验收标准 `必填`

### AC 覆盖策略基线 `必填`

> 生成 AC 前必须先根据 `strategies/be-testcase-strategy.md` 识别 Story 类型，并叠加对应覆盖策略。AC 不等于测试用例全集，但必须覆盖所有核心验收维度；完整测试步骤在 TestCase 文档中展开。

| Story 类型 | 是否适用 | 判断依据 | 必须覆盖的 AC 维度 |
|---|---|---|---|
| 状态机 | 是 / 否 |  | 合法流转、非法流转、初始态、终态、多步序列、并发/幂等 |
| CRUD | 是 / 否 |  | 正常、边界、业务规则拒绝、权限、关联数据不存在 |
| 回调/Webhook | 是 / 否 |  | 签名、幂等、数据一致、字段校验、异常恢复 |
| 定时任务 | 是 / 否 |  | 正常、空跑、阈值边界、并发、单条失败处理 |
| 集成/编排 | 是 / 否 |  | 全成功、逐环节失败、回滚、超时、降级 |

| 覆盖维度 | 是否适用 | 对应 AC ID | 缺口说明 |
|---|---|---|---|
| 字段边界 | 是 / 否 |  |  |
| 权限 / 鉴权 | 是 / 否 |  |  |
| 并发 / 幂等 | 是 / 否 |  |  |
| 关联数据状态 | 是 / 否 |  |  |
| 多步序列 | 是 / 否 |  |  |
| 字段组合 | 是 / 否 |  |  |
| 前端可见行为 | 是 / 否 |  | 错误码展示、空态、弱网、超长字段等 |

> 缺口说明只能填写“不适用依据”或“需人工确认的问题”；不得填写“后续补充”后继续进入 Review。

### AC 明细

| AC ID | Given | When | Then | 测试层级 | 测试实现 |
| --- | --- | --- | --- | --- | --- |
| AC-001 |  |  |  | 单元 / 接口 / 集成 | |

> **AC 最小覆盖维度（编写时逐项检查）：**
>
> 1. **正向流程** — 主路径走通，输出正确（每个主流程至少一条）
> 2. **业务规则拒绝** — 每条异常流程中的业务规则被违反时的拒绝行为
> 3. **入参边界** — 必填字段缺失、格式非法、长度越界等校验失败场景
> 4. **异常容错** — Story 中声明"失败不阻塞主流程"的依赖，需验证其失败时主流程仍正常返回
> 5. **幂等 / 并发**（如涉及）— 重复请求的预期行为、并发竞争的正确性保证
> 6. **🔴 前端 UI 展示**（如涉及）— 状态/枚举在前端的展示正确（颜色/图标/文案）
> 7. **🔴 前端交互**（如涉及）— 表单提交、按钮置灰、防抖节流、空态/错态展示
> 8. **🔴 前端错误码处理**（如涉及）— 每个错误码的前端展示和处理逻辑正确
> 9. **类型策略维度** — 状态机 / CRUD / 回调 / 定时任务 / 集成编排中适用的策略维度均已映射到 AC
> 10. **通用测试维度** — 字段边界、权限、并发、关联数据状态、多步序列、字段组合中适用的维度均已映射到 AC

> 示例：
>
> | AC ID | Given | When | Then | 测试层级 | 测试实现 |
> | --- | --- | --- | --- | --- | --- |
> | AC-001 | 用户存在且 nickname 合法 | 提交 nickname=新昵称 | 返回 200，用户昵称更新成功 | 接口 | ✅ UserServiceTest.updateProfile_success |
> | AC-002 | userId 对应用户不存在 | 提交合法 nickname | 返回错误码 11001 | 接口 | ✅ UserServiceTest.updateProfile_userNotFound |
> | AC-003 | 用户存在 | 提交空白 nickname | 返回错误码 10001 | 接口 | ✅ UserServiceTest.updateProfile_invalidNickname |
> | AC-004 🔴 | 用户已有昵称 | 打开用户详情页 | UI 展示最新昵称 | L5 组件 | ✅ UserProfileName.test.ts display |
> | AC-005 🔴 | 表单填写中点击提交按钮 | 提交按钮 | 按钮立即置灰 3 秒 + Loading 状态 | L5 组件 | ✅ ProfileForm.test.ts preventDoubleSubmit |

>
> **测试实现列填写规范：**
> - 格式：`{测试类名}.{测试方法名}` 或 `✅ {简要说明}`
> - 状态：✅（已完成）/ ⏸️（需联调/不可测试）/ ⬜（待实现）
> - 示例：✅ ImMessageAppServiceTest.handleCallback_idempotent

---

## 用例设计映射 `必填（Story 完成时同步规划，TC 编写完成后更新自动化状态）`

| AC ID | 用例 ID | 用例文档 | 测试文件 | 自动化状态 |
| --- | --- | --- | --- | --- |
| AC-001 | TC-STORY-001-001 |  |  | Planned |

> **测试文件列填写规范：**
> - 格式：`{模块名}/{测试类}.java`
> - 示例：`im-message/ImMessageAppServiceTest.java`
>
> **🔴 前端测试文件格式：** `{模块名}/{测试类}.test.ts` 或 `{模块名}/{测试类}.spec.ts`
>
> **自动化状态：** Planned（规划中）→ ✅ Pass（已实现并通过）→ ⏸️ Skip（跳过）→ 🔴 Fail（失败）

---

## 实现任务映射 `必填`

> 按实现顺序排列，序号即执行顺序。有依赖关系的 Task 必须标注前置依赖。每个 Task 必须对应“实现方案决策基线”的实现点和能力归属。

| # | Task | 说明 | 涉及工程 / 层 | 能力归属 | 复用对象 | 负责角色 | 前置依赖 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 |  |  | {工程名} / BFF · SPI · Service · Domain · **🔴 FE-Web · FE-MP** |  |  | AI / 人工 | — | Planned |
| 2 |  |  |  |  |  | AI / 人工 | Task 1 | Planned |

> 示例：
>
> | # | Task | 说明 | 涉及工程 / 层 | 能力归属 | 复用对象 | 负责角色 | 前置依赖 | 状态 |
> | --- | --- | --- | --- | --- | --- | --- | --- | --- |
> | 0 | 公共依赖说明 | DO、Mapper、Repository 接口、公共类包路径 | icec-cloud-life-cs / Infrastructure · Domain | 公共依赖 | 现有 BaseRepository / 错误码规范 | AI | — | Planned |
> | 1 | SPI 层定义 | SPI 接口、DTO、Feign Client | icec-cloud-life-cs / SPI | 对外契约 | 复用项目 SPI 命名和返回结构 | AI | Task 0 | Planned |
> | 2 | Domain 层实现 | 聚合根、领域服务、状态机 | icec-cloud-life-cs / Domain | 状态流转唯一 owner | 复用/扩展现有状态机模式 | AI | Task 0 | Planned |
> | 3 | Application 层编排 | AppService 编排、事务控制 | icec-cloud-life-cs / Application | 业务编排 | 调用 Task 2，不重复实现状态流转 | AI | Task 1, 2 | Planned |
> | 4 | Infrastructure 层实现 | Repository 实现、Mapper XML | icec-cloud-life-cs / Infrastructure | 存取能力 | 复用现有 Mapper/Converter 模式 | AI | Task 0, 2 | Planned |
> | 5 | Interfaces 层接口 | Controller、参数校验 | icec-cloud-boss-bff / BFF | 协议适配 | 调用 Task 3，不写业务规则 | AI | Task 1, 3 | Planned |
> | 6 | 单元测试 | 核心逻辑测试 | icec-cloud-life-cs / Test | 测试支撑 | 复用测试基类/Fixture | AI | Task 2, 3, 4 | Planned |
> | 7 🔴 | 前端接口对接 | TypeScript Interface 定义、API Client 封装 | icec-cloud-boss-web / FE-Web | 前端契约适配 | 复用现有 API Client 封装 | AI | Task 1, 5 | Planned |
> | 8 🔴 | 前端组件实现 | 状态展示组件、表单组件 | icec-cloud-boss-web / FE-Web | 前端展示 | 复用现有组件库 | AI | Task 7 | Planned |
> | 9 🔴 | 前端组件测试 | Vitest 单元测试 | icec-cloud-boss-web / Test | 测试支撑 | 复用测试工具链 | AI | Task 7, 8 | Planned |
> | 10 🔴 | 前端 E2E 测试 | Playwright 端到端测试 | icec-cloud-boss-web / E2E | 端到端验证 | 复用 E2E 登录/数据夹具 | AI | Task 8 | Planned |

> **重复实现红线：** 若多个 Task 涉及同一业务能力（如结单/关闭/状态流转/通知），必须指定唯一 owner Task，其余 Task 只能调用或编排，不得复制业务逻辑。

---

## 偏离声明 `选填（无偏离时可省略）`

> 只写偏离 constraints 默认行为的特殊情况。
> 默认行为：需要鉴权、AppService 层开启本地事务、禁止在事务中调用 Feign。

-

> 示例：
> - 鉴权：此接口加 `@SkipAuth`，无需登录即可访问（公开查询接口）
> - 事务：涉及跨服务数据一致性，需使用分布式事务（Seata AT 模式）

---

## 第三方服务 `选填（有调用外部/第三方服务时填写）`

> **前置要求：** 填写第三方服务前，必须已在“实现方案决策基线”中完成对应实现点的现有能力复用扫描。业务 Story 不得默认直连第三方 API；若团队已有中台、SDK 封装、Gateway、Repository 或历史实现，必须优先复用或说明不复用理由。

| 服务名称 | 调用方式 | 开发文档地址 | 负责人 / 联系方式 |
| --- | --- | --- | --- |
|  | REST / SDK / MQ |  |  |

> 示例：
>
> | 服务名称 | 调用方式 | 开发文档地址 | 负责人 / 联系方式 |
> | --- | --- | --- | --- |
> | 通知中台（内部封装极光/站内信等） | SPI / Feign / MQ | 见项目资产或现有代码 | @李四 |
> | 融云 IM  | SDK      | https://docs.rongcloud.cn/platform-chat-api/server-sdk     |                   |
>
> SDK 坐标：`cn.rongcloud.im:server-sdk-java:4.0.2`（[GitHub](https://github.com/rongcloud/server-sdk-java)）

---

## 依赖与风险 `选填`

- 上游依赖：
- 下游影响：
- 风险：
- 缓解方式：
- **🔴 前端依赖：**（如：依赖前端组件库版本 / 依赖前端权限申请 / 依赖 mock 平台就绪）
- **🔴 前端风险：**（如：iOS 旧版本兼容 / Web 浏览器兼容 / 弱网下体验降级）

> 示例：
> 上游依赖：无
> 下游影响：用户状态变为 INACTIVE 后，该用户的登录请求将被拒绝
> 风险：并发场景下可能出现重复更新，需确认幂等性要求
> 前端依赖：依赖 Boss 后台 v2.5+ 组件库（需先升级），依赖 yapi mock 平台（项目 ID: 123）
> 前端风险：iOS 12 以下 WebView 不支持 AbortController，需降级处理

---

## 未决问题 `选填`

| 问题 | 负责人 | 截止时间 | 状态 |
| --- | --- | --- | --- |
|  |  |  | Open |
| **🔴 {前端未决问题}** | **@{前端}** |  | Open |

> 示例：停用用户时是否需要同步踢出已登录的 Token？影响：需调用认证服务。负责人：@张三
> 🔴 前端未决：状态展示组件是否需要支持深色模式？负责人：@前端张三

---

## 🔴 前端联调信息 `必填（有前端对接时）`

> 所有联调相关信息的集中入口，前端拿到本章节即可启动联调工作。

### 联调基础信息

| 项目 | 内容 |
|------|------|
| **Story ID** | STORY-XXX-BE |
| **前端对接方（团队）** |  |
| **前端 Owner** | @ |
| **后端 Owner** | @ |
| **联调时间窗口** | YYYY-MM-DD ~ YYYY-MM-DD |
| **联调优先级** | P0 / P1 / P2 |
| **预计联调工时** | X 人日 |

### 联调环境

| 环境 | 后端 URL | 前端 URL | 账号 / 备注 |
|------|---------|---------|-----------|
| 开发环境 | http://localhost:8080 | http://localhost:3000 | dev_user / Dev@123 |
| 测试环境 | https://test-api.example.com | https://test-web.example.com | test_user / Test@123 |
| 预发布环境 | https://pre-api.example.com | https://pre-web.example.com | pre_user / Pre@123 |

### Mock 与调试

| 项目 | 内容 |
|------|------|
| **Mock 平台** | yapi（项目 ID: 123，URL: https://yapi.example.com/project/123） |
| **Mock 负责人** | @ |
| **Mock 完成时间** | YYYY-MM-DD |
| **是否提供 Postman / Apifox 集合** | 是（路径: docs/api/{STORY-ID}.postman.json） |
| **联调抓包工具** | Charles / Fiddler / 浏览器 DevTools |
| **日志查看** | https://grafana.example.com/d/{dashboard-id} |

### 前端技术栈

| 项目 | 内容 |
|------|------|
| **前端框架** | Vue 3 / React 18 / 小程序 |
| **UI 组件库** | Element Plus / Ant Design / Vant |
| **HTTP Client** | axios / fetch |
| **状态管理** | Pinia / Vuex / Redux / Zustand |
| **TypeScript** | 是 / 否 |
| **测试框架** | Vitest + Vue Test Utils / Jest + React Testing Library |

### 兼容性要求

| 项目 | 要求 |
|------|------|
| **浏览器兼容** | Chrome 90+ / Edge 90+ / Safari 14+ |
| **APP 兼容** | iOS 13+ / Android 7+ |
| **是否需要并行支持老接口** | 是（灰度 X 天）/ 否 |
| **国际化** | zh-CN / en-US / 需新增语种：____ |

### 联调 Checklist

- [ ] Mock 已就绪（前端可独立开发）
- [ ] 后端测试环境已部署
- [ ] 接口联调通过（每个接口至少一个完整 happy path）
- [ ] 错误码前端处理已实现并验证
- [ ] 状态流转前端展示已实现并验证
- [ ] 边界场景前端处理已实现并验证
- [ ] 性能要求已达标（参考"前端性能与体验"章节）
- [ ] 兼容性已验证（参考"兼容性要求"）
- [ ] 前端组件测试通过（L5）
- [ ] 前端 E2E 测试通过（L6）

---

## 验收记录 `选填（Story 完成时填写）`

> 支持拆分验收：可按实现任务分批验收，不必等待全部完成。不可测试的 AC 需标注原因。

### 拆分边界

**可拆分的验收单元：**
- 按实现任务拆分（如：DDL/PO/Mapper → 接口实现 → 业务逻辑 → 集成测试）
- 按依赖拆分（如：纯内部逻辑 → 依赖外部系统 → 端到端）
- 按测试层级拆分（如：单元测试 → 接口测试 → 集成测试）
- **🔴 按前后端拆分**（如：后端接口 → 前端对接 → 联调验证）

**不可拆分的最小单元：**
- 单个 AC 不可拆分，必须作为一个整体验收
- 同一批次的 AC 应同时验收或同时 Skip

### 拆分原则

| 原则 | 说明 |
|------|------|
| 依赖驱动 | 前置批次通过后，才能启动后续批次 |
| 独立可验 | 每批次应能独立验证，不依赖未实现的代码 |
| 风险隔离 | 高风险项（外部依赖、集成点）放后期批次，尽早暴露问题 |
| 增量价值 | 每批次完成应产生可用的功能增量 |
| **🔴 前后端解耦** | **前端可用 mock 独立验收，后端用接口测试独立验收，联调作为最终验收** |

### 验收批次

| 批次 | 验收范围 | 前置批次 | 验收日期 | 验收人 | 状态 |
|------|---------|---------|---------|--------|------|
| 1 | | — | | | ⬜ 待验收 |
| 2 🔴 | 前端组件 + 联调 | 1 | | | ⬜ 待验收 |

### AC 完成情况

| AC ID | 验收批次 | 验证方式 | 验证人 | 验证日期 | 状态 | 备注 |
|-------|---------|---------|--------|---------|------|------|
| AC-001 | 1 | | | | ⬜ Pending | |
| AC-002 | 1 | | | | ⬜ Pending | |

**状态说明：** ✅ Pass（通过）| 🔴 Fail（失败）| ⏸️ Skip（跳过/不可测试）| ⬜ Pending（待验收）

### 跳过的 AC（不可测试原因）

| AC ID | 所属批次 | 跳过原因 | 负责人 | 预计补测时间 |
|-------|---------|---------|--------|-------------|
| | | | | |

### 测试用例执行

| 用例 ID | 批次 | 执行结果 | 执行人 | 执行日期 | 备注 |
|---------|------|---------|--------|---------|------|
| TC-STORY-XXX-001 | 1 | ⬜ Pass / 🔴 Fail / ⏸️ Skip | | | |

### 人工任务完成

| Task | 批次 | 状态 | 完成日期 | 备注 |
|------|------|------|---------|------|
| | 1 | ⬜ Pending | | |
