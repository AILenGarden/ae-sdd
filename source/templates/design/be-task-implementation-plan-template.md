# {STORY-ID} Task 实现方案

> **用途：** Task 生成完成后，输出完整的实现方案汇总，作为开发者的实施蓝图。
> 审核者/开发者基于此表快速了解：要做哪些事、怎么做、依赖什么、顺序是什么。
>
> **前置条件：** 所有 Task 文档（Task-0 至 Task-N）已生成并通过一致性校验。

---

## 1. Story 信息 `必填`

| 字段 | 内容 |
| --- | --- |
| Story ID | {STORY-ID} |
| Story 标题 | {标题} |
| 涉及工程 | {工程列表} |
| Task 总数 | {N} 个 |
| 生成时间 | {YYYY-MM-DD HH:mm} |

---

## 2. Task 清单与执行顺序 `必填`

| # | Task ID | 名称 | 涉及工程/层 | 前置依赖 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 0 | Task-0 | 公共依赖说明 | {工程}/{层} | — | {Planned/Generated} |
| 1 | Task-1 | {名称} | {工程}/{层} | Task 0 | {Planned/Generated} |
| 2 | Task-2 | {名称} | {工程}/{层} | Task 0, 1 | {Planned/Generated} |

---

## 3. Task 执行顺序图 `必填`

```
Task 0（公共依赖）
    │
    ├── Task 1（SPI 层）
    │       │
    │       └── Task 3（Application 层）
    │               │
    └── Task 2（Domain 层）
            │
            └── Task 4（Infrastructure 层）
                    │
                    └── Task 5（Interfaces 层）
                            │
                            └── Task 6（单元测试）
```

---

## 4. 各 Task 核心实现要点 `必填`

### Task-0：公共依赖说明

**职责：** 定义公共类包路径、DO 定义、Repository 接口、Gateway 接口、技术栈版本。

**关键产出：**
- 公共包路径：`{package.path}`
- DO 实体：`{DO 名称}`
- Repository 接口：`{Repository 接口名}`
- Gateway 接口：`{Gateway 接口名}`

---

### Task-1：{名称}

**涉及工程/层：** {工程}/{层}

**前置依赖：** Task 0

**核心实现逻辑：**

> 描述本 Task 的核心实现逻辑（2-5 句话）。

**核心代码片段：**

```java
// 关键代码
```

**DB 操作：**
| 操作 | 表 | 字段 | 事务范围 |
| --- | --- | --- | --- |
|  |  |  |  |

**约束检查：**
- [ ] {约束项}

---

### Task-2：{名称}

（同上格式，按实际 Task 数量复制）

---

## 5. 分层实现分工 `必填`

> **🔴 强制：** 分层职责红线：Domain 写领域逻辑，Application 写业务编排，Repository 只做数据存取，不串味（依据 `constraints/project-structure.md`）。下表"本层职责落点"列必须写清各层放了什么，便于核对是否归位。

| 层级 | 负责的 Task | 关键类/接口 | 本层职责落点（写什么/不写什么） |
| --- | --- | --- | --- |
| SPI | Task-1 | {类名} | 对外接口契约定义 |
| Domain | Task-2 | {类名} | 领域逻辑：状态流转规则、业务不变量、领域计算（充血模型）。❌ 不写编排/SQL |
| Application | Task-3 | {类名} | 业务编排：调用领域服务、组织事务边界、协调多 Repository/SPI、DTO↔DO。❌ 不写领域规则 |
| Infrastructure | Task-4 | {类名} | Repository 实现只做存取（findByXxx/save/update）+ PO↔DO 转换。❌ 不写业务/领域逻辑 |
| Interfaces | Task-5 | {类名} | 协议适配、参数校验（@Valid）、调用 Application |

---

## 6. 接口实现映射 `必填`

| Story 接口契约 | 对应 Task | 实现类 | 方法 |
| --- | --- | --- | --- |
| {REST/SPI 接口描述} | Task-N | {类名} | {方法名} |

---

## 7. DB 变更清单 `选填（条件）`

| # | 表 | 变更类型 | 涉及字段 | 对应 Task |
| --- | --- | --- | --- | --- |
| 1 |  |  |  |  |

**DDL（如有）：**

```sql
-- {表名}
```

---

## 8. 外部依赖 `选填（条件）`

| # | 依赖项 | 接口 | 调用方 | 失败策略 |
| --- | --- | --- | --- | --- |
| 1 |  |  |  |  |

---

## 9. 事务边界 `必填`

```
┌─────────────────────────────────────────────────┐
│  @Transactional 内（同一事务）                     │
│  ···                                            │
└─────────────────────────────────────────────────┘
         ↓ 事务提交
┌─────────────────────────────────────────────────┐
│  异步（不在事务内）：                               │
│  ···                                            │
└─────────────────────────────────────────────────┘
```

---

## 10. 约束合规检查清单 `必填`

| # | 约束项 | 规范要求 | 实现方式 | 是否合规 |
| --- | --- | --- | --- | --- |
| 1 |  |  |  | ✅ |

---

## 11. 与 Story 接口契约的一致性确认 `选填（条件）`

| 接口 | Story 定义的方法签名 | Task 实现的方法签名 | 是否一致 |
| --- | --- | --- | --- |
|  |  |  | ✅ / ❌ |

---

## 12. Task 间依赖与调用关系 `必填`

```
{按调用顺序列出所有 Task 间的调用关系}
```

---

## 13. 实现注意事项 `必填`

| # | 注意事项 | 风险等级 | 说明 |
| --- | --- | --- | --- |
| 1 |  | 🟠 |  |

**以上为 {STORY-ID} 完整实现方案。Task 文档已生成，是否确认开始 Coding？**
