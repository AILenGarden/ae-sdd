# Task {Story ID}-{序号}：{Task 标题}

## 元信息 `必填`

- Task ID：Task {序号}
- 来源 Story：[{Story ID}]({Story 文档相对路径})
- 涉及工程：{工程名} / {层}
- 状态：Pending / In Progress / Done
- 处理人：

---

## 依赖关系 `必填`

**前置依赖：**
- Task {ID}：{说明}

> 实现前必读：[Task 0 - 公共依赖说明](./xxx-task-0-公共依赖说明.md)（包路径、import、技术栈版本）

**被依赖：**
- Task {ID}：{说明}

---

## 包路径约定 `必填`

| 层 | 包路径 |
| --- | --- |
| | `com.casstime.cloud.life.{module}.{layer}.{package}` |

---

## 任务描述 `必填`

{一句话描述本 Task 的职责}

---

## 核心设计 `必填`

### 对应 Story 实现方案决策基线 `必填`

> 本 Task 对应 Story `## 实现方案决策基线` 中的哪个实现点？复用了哪个现有能力？或为什么新建？

{填写对应点 + 复用/新建依据}

### 关键设计机制 `必填`

> **🔴 硬约束：** 本节属于**代码设计判断**（状态机 / 幂等 / 锁 / MQ / 补偿），必须由 `CodingSkill.Plan(task-level)` 在加载 CodingModel 后做出决策。  
> **TaskSkill 不得自行填写本节。**  
> **填写依据：** 本节是 CodingModel 维度①原子性 / 维度②并发安全 / 维度③幂等性 / 维度④同步异步解耦 的具体化落地；后续 `## CodingModel 决策记录` 中对应维度的"处理方案"列需与本节一致。

{状态机 / 幂等 / 乐观锁 / 分布式锁 / MQ / 补偿等}

---

## CodingModel 决策记录 `必填`

> **本节由 `CodingSkill.Plan(task-level)` 生成，TaskSkill 不得自行填写或删减。**  
> 任一维度结论为空或"不知道"，禁止进入实现。

| 维度 | 本 Task 结论 | 处理方案 | 证据（Story AC / TestCase / 文件:行号）|
| --- | --- | --- | --- |
| ① 原子性 | 需要 / 不需要 | 事务边界 / TCC / 无 | |
| ② 并发安全 | 需要 / 不需要 | 乐观锁 / 分布式锁 / 无 | |
| ③ 幂等性 | 需要 / 不需要 | 幂等键 / 唯一索引 / 状态机 / 无 | |
| ④ 同步/异步解耦 | 同步 / 异步 | MQ / Outbox / 线程池 / 无 | |
| ⑤ 数据一致性 | 强一致 / 最终一致 | 本地事务 / 补偿 / 无 | |
| ⑥ 外部依赖容错 | 有 / 无 | 超时 / 重试 / 降级 / 无 | |
| ⑦ 性能瓶颈 | 有 / 无 | 索引 / 批量 / 限流 / 无 | |
| ⑧ 资源隔离 | 需要 / 不需要 | 独立线程池 / 分级队列 / 无 | |
| ⑨ 安全 | 需要 / 不需要 | 鉴权 / 加密 / 参数校验 / 无 | |
| ⑩ 可观测性 | 已覆盖 / 未覆盖 | 日志 / Metrics / Trace / 告警 | |
| ⑪ 可运维性 | 已覆盖 / 未覆盖 | 开关 / 回滚 / 灰度 / 无 | |

### 核心链路保护 `选填（涉及回调/支付/状态更新/消息落库/通知/批量任务时必填）`

| 检查项 | 结论 | 方案 |
| --- | --- | --- |
| 本 Task 是否处于核心实时链路 | 是 / 否 | |
| 是否与批量/离线任务共享线程池 | 是 / 否 / 不涉及 | |
| 是否与批量/离线任务共享队列/Topic | 是 / 否 / 不涉及 | |
| 是否与批量/离线任务共享 DB 连接池或热点表 | 是 / 否 / 不涉及 | |
| 队列是否有上限 | 是 / 否 / 不涉及 | |
| 拒绝策略是否反压核心线程 | 是 / 否 / 不涉及 | |
| 是否有补偿/重试/DLQ | 是 / 否 | |
| 是否有积压/延迟/失败告警 | 是 / 否 | |
| 是否有批量任务满载 + 核心接口并发混合压测 | 是 / 否 / 不涉及 | |

---

## 任务级 CodePlan `必填`

> **本节由 `CodingSkill.Plan(task-level)` 生成。Coding SKILL 按此骨架"填肉"写出完整代码。**

### 类骨架 `必填`

| 类名 | 层 | 包路径 | 职责一句话 |
| --- | --- | --- | --- |
| | | | |

**各类骨架（注解 + 字段 + 方法签名 + 伪代码 ≤10 行）：**

```java
// 注解 + 类签名
@Slf4j
@Service
@RequiredArgsConstructor
public class {ClassName} {

    // 核心依赖（Repository / Client / 外部 Service）
    private final {DepType} {depName};

    /**
     * {方法职责一句话}
     */
    public {ReturnType} {methodName}({ParamType} {param}) {
        // 1. {动词} {做什么}（如：校验 xxx 不为空）
        // 2. {动词} {做什么}（如：查询 XxxDO by id）
        // 3. {动词} {做什么}（如：调用 XxxClient.method() 获取外部数据）
        // 4. {动词} {做什么}（如：转换并返回 XxxVO）
    }
}
```

> **骨架规范：**
> - 注解必须完整（`@Slf4j` / `@Service` / `@RequiredArgsConstructor` / `@Transactional` 等）
> - 字段只列核心依赖，不列工具类
> - 方法伪代码每步以动词开头，≤10 行
> - **禁止写完整方法体**（条件分支、循环、try-catch 由 Coding SKILL 填充）

### 依赖工具包 `选填`

| 依赖 | 类 / 注解 | 用途 |
| --- | --- | --- |
| {框架/库} | `{ClassName}` / `@{Annotation}` | {用途一句话} |

### 方法级逻辑 `必填`

| 类 | 方法 | 输入 | 输出 | 逻辑步骤（动词开头） | 异常处理 |
| --- | --- | --- | --- | --- | --- |
| | | | | | |

### DB 操作 `选填`

| 操作 | 表 | WHERE / 幂等键 | 事务边界 | 并发控制 |
| --- | --- | --- | --- | --- |
| | | | | |

### 外部依赖 `选填`

| 依赖 | 调用方式 | 超时 | 重试 | 降级 | 是否阻塞主流程 |
| --- | --- | --- | --- | --- | --- |
| | | | | | |

### 测试映射 `必填`

| TestCase ID | 场景 | 本 Task 覆盖点 | 是否需要真实 DB/HTTP |
| --- | --- | --- | --- |
| | | | |

---

## 约束检查 `必填`

| 约束文档 | 检查项 |
| --- | --- |
| layered-arch | 无跨层调用；事务只在 Application 层 |
| code-style | 无魔法值；命名规范 |
| database | 索引已设计；无裸 SQL 拼接 |
| | |

---

## 验收映射 `必填`

> 本 Task 实现后，哪些 Story AC 可以被满足。

| Story AC | 本 Task 实现点 |
| --- | --- |
| AC-{N} | {哪个方法/逻辑覆盖此 AC} |

---

## 验收签字 `必填`

- 开发负责人：__________ 日期：__________
