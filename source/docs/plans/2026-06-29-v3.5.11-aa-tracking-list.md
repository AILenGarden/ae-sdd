# ae-sdd 全维对齐追踪清单（🆕 v3.5.11 AA 首跑 gap 留痕 → v3.5.12 全部收敛）

> **来源：** 2026-06-29 AA（全维对齐验证器）首次全量扫描产出。
> **性质：** 🟡 持续追踪清单——本档记录的 gap **不阻断 v3.5.11 启用**（已诚实降级为 warn），
> 但纳入 AA 的 UC-10/UC-11 持续追踪，逐个迭代收敛。
> **🟢 v3.5.12 更新：全部 gap 已收敛！** UC-10 死字段 15→0、UC-11 状态机 3→0。
> 详见 v3.5.12 CHANGELOG。
> **复核方式：** `ae-sdd update-check` → UC-10/UC-11 现已全 OK。
> **根治路径：** 第 1 波 review-loop CLI（v3.5.12 ✅）+ 第 2 波 PRD 子系统（v3.5.12 ✅）。

---

## 一、AA 5 维扫描结果（v3.5.11 首跑）

| 维度 | 检查 ID | 首跑结果 | 档位 |
|------|---------|---------|------|
| 门禁承诺↔注册双向 | UC-08 | 189 处软门禁未绑定 G-XX | 🟠 软门禁（已在 5 个核心 SKILL 加强度声明）|
| 门禁实现真实性 | UC-09 | ✅ 已修（G-RA-FLOW-VIOLATION 假门禁已修复）| 🔴 → ✅ |
| state 字段存活性 | UC-10 | 15 个死字段 | 🟡 死字段追踪档 |
| 状态机闭环 | UC-11 | 3 个状态机未持久化 | 🟡 状态机闭环档 |
| 幽灵命令全捕获 | UC-12 | ✅ 已清（run dr-review-skill 已删除）| 🔴 → ✅ |

---

## 二、🟡 UC-10 死字段清单（15 个，按优先级）

> **定义：** doc schema（SKILL/document-storage）承诺的字段，但 `tools/lib/` 无写入方。
> AI 被要求维护这些字段，但 compact/重入后丢失，等于靠记忆。

### P1 — 核心流程死字段（影响多 Agent / 重入）

| # | 字段 | doc 承诺位置 | 期望写入方 | 处置计划 |
|---|------|------------|-----------|---------|
| 1 | `activeAgents[]` | SKILL.md §🤖 多 Agent 状态共享 | sub-agent 派活/完成时写 | 第 3 波：review-loop CLI 复用 |
| 2 | `agentReports[]` | SKILL.md §🤖 多 Agent 状态共享 | sub-agent 完成时移入 | 第 3 波 |
| 3 | `codingRound` | SKILL.md §流程状态跟踪 | 每轮 Coding 前累加 | 第 3 波 |
| 4 | `completedSteps` | SKILL.md §重入流程判定 | 完成步骤后 append | 第 3 波（注意：classify.py 只读做规模推断）|
| 5 | `currentStep` | SKILL.md §重入流程判定 | 进入新步骤时写 | 第 3 波 |

### P2 — PRD 级死字段（影响 PRD 完成判定）

| # | 字段 | doc 承诺位置 | 期望写入方 | 处置计划 |
|---|------|------------|-----------|---------|
| 6 | `prdId` | document-storage §3.5 | prd-init 命令（**不存在**）| 后续迭代：补 prd-init 或删字段 |
| 7 | `crossStoryDeps[]` | document-storage §3.5 | Story 完成 hook | 后续迭代 |
| 8 | `crossStoryResidualRisks[]` | document-storage §3.5 | Story 完成 hook | 后续迭代 |
| 10 | `prdReview{}` | document-storage §3.5 | 审核点 5 确认 | 后续迭代（注意 session.py 写的是 userConfirmedPhases，非 prdReview）|
| 11 | `gateRegistry{}` | document-storage §3.5 | check_prd_4_layers 写回 | 后续迭代 |
| 12 | `memoryLifecycle{}` | document-storage §3.5 | memory_gate.py（**实际读 memory_store 不读此字段**）| 后续迭代：删字段或对接 |
| 13 | `compactHistory[]` | document-storage §3.5 | prd-complete append | 后续迭代 |
| 14 | `sizeBudget{}` | document-storage §3.5 | prd-init | 后续迭代 |
| 15 | `storyIds[]` | document-storage §3.5 | Story 完成 hook | 后续迭代 |

### 已知孤儿函数（半活）

| 函数 | 位置 | 问题 |
|------|------|------|
| `check_prd_4_layers` | state.py:222 | 读 7 个 PRD 子字段做 G-PRD-1~4 校验，但字段无写入方 → 永远 fail（除非手写 JSON）|

---

## 三、🟡 UC-11 状态机未闭环清单（3 个）

> **定义：** doc 承诺的「连续 N 轮 / 累加计数 / 状态机 N 态」无 tools/ 持久化实现。

| # | 承诺语义 | doc 位置 | 持久化状态 | 处置计划 |
|---|---------|---------|-----------|---------|
| 1 | 连续 3 轮无新增 | review-loop 公共协议 §协议1 | ❌ 无 dryCounter 字段 | **第 1 波 review-loop CLI 落地后自动收敛** |
| 2 | 循环上限 | review-loop 公共协议 §协议2 | ❌ 无 max_rounds | 第 1 波收敛 |
| 3 | reviewerTier | agent-orchestration §8.4.1 | ❌ 无 compute_tier | 第 1 波收敛（Tier 机械派生）|
| (4) | prdStatus 5 态 | document-storage §3.5 | 🟡 部分（仅 awaiting_compact 能写）| 后续迭代（第 3 波）|

> **注：** #1~3 会在原计划第 1 波 review-loop CLI（方案 E）落地后自动从 warn 收敛为 ok，
> 因为 review-loop CLI 会建 reviewState/dryCounter/tier 字段。这是 AA 与第 1 波的衔接点。

---

## 四、处置原则

1. **本清单不阻断 v3.5.11 启用**——AA 已把它们降级为 warn，全量 update-check 仍全绿。
2. **AA 自动追踪**——每次 `ae-sdd update-check` 跑 UC-10/UC-11，死字段/状态机收敛后 warn 自动清零。
3. **处置优先级**：第 1 波（review-loop CLI，收敛 #状态机 1-3）> 第 3 波（多 Agent state，收敛 #死字段 1-5）> 后续迭代（PRD 级，#死字段 6-15）。
4. **禁止引入新死字段**：未来新增 doc schema 字段时，必须同步补 tools/ 写入方，否则 AA 首跑即报。

---

## 五、相关文件

- AA 实现：`tools/lib/alignment_audit.py`
- AA 测试：`tools/tests/test_alignment_audit.py`
- AA 接入：`tools/bin/ae-sdd:cmd_update_check`（UC-08~12 统一调度）
- 首跑诊断报告：本文件
- CHANGELOG：`source/CHANGELOG/2026-06-29-v3.5.11-alignment-audit.md`
