<!--
  Drop-in JaCoCo plugin block. Insert inside <project><build><plugins>...</plugins></build>.
  Produces both XML (jacoco.xml — required by this skill) and HTML reports under
  target/site/jacoco/ when `mvn test` runs. JDK target up to 21.
-->
<plugin>
    <groupId>org.jacoco</groupId>
    <artifactId>jacoco-maven-plugin</artifactId>
    <version>0.8.12</version>
    <executions>
        <execution>
            <id>prepare-agent</id>
            <goals>
                <goal>prepare-agent</goal>
            </goals>
        </execution>
        <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
                <goal>report</goal>
            </goals>
            <configuration>
                <formats>
                    <format>XML</format>
                    <format>HTML</format>
                </formats>
            </configuration>
        </execution>
    </executions>
</plugin>
