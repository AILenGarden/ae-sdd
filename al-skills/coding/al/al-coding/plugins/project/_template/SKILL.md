# {project-key} — {项目名} 项目适配器（模板）

> **定位：** 叠加于 core、语言插件和架构插件之上的项目事实层。只放本项目事实：版本锁定、包根、命名指针、踩坑库、项目约束和覆盖项。通用知识引用对应 Core 或适配 SKILL（见 `framework/plugin-spec.md`）。
>
> **参考范例：** ae-sdd 的 `plugins/java3d-coding-skill`（icec/life 项目族适配器，含成熟的技术栈事实表/踩坑库形态，可作为填写样例；其内容属 icec 项目族，如需引入请为 life/icec 建正式项目插件并从该文件摘取事实）。

## §0 项目档案

| 项 | 值 |
|----|----|
| 仓库根 | `D:\Path\To\YourProject` |
| 语言/框架 | java + spring-boot（→ 命中语言插件 java-spring-coding） |
| 架构 | ddd（→ 命中架构插件 ddd） |
| 约束文档源 | `{仓库}/docs/constraints/` 或 AGENTS.md（§2 指针用） |

## §1 技术栈版本锁定（CodingModel 设计阶段决策前提）

> 版本敏感事实表：每行一个"随版本才能定"的事实。事实与"为什么"均记录，不写通用知识。

| 维度 | 锁定事实 | 决策影响 |
|------|---------|---------|
| 语言版本 | Java 8 | 禁用 9+ 语法 |
| 框架版本 | Spring Boot x.y.z | 校验注解包来源、Feign 注解包来源 |
| 消息/缓存/DB | （本项目用什么） | （选型决策约束） |

## §2 项目约束指针（CodingModel 上下文输入的落地源）

| 约束维度 | 本项目权威文档 |
|---------|--------------|
| 技术栈 | `constraints/technology-stack.md` |
| 工程结构 | `constraints/project-structure.md` |
| 代码风格（含日志格式） | `constraints/code-style.md` |
| 接口规范 / 数据库 / 安全 / 测试 | （逐项列出） |

## §3 包根与目录事实

| 项 | 值 |
|----|----|
| base package | `com.yourcompany.yourproject` |
| 模块划分 | （列出模块及职责一句话） |
| 包路径拼法 | base + `.{domain}` + `.{层}` （层内结构见架构插件） |

## §4 覆盖项（对 core/上游插件的收窄，逐条声明理由）

| 覆盖 | 内容 | 理由 |
|------|------|------|
| CodingModel 测试/验证章节 | （如：本项目真实协议验证命令改为…） | 历史结构原因 |

## §5 踩坑决策库（本项目实测教训，"遇到 X 时怎么选"）

| # | 场景 | 踩坑 | 决策 |
|---|------|------|------|
| 1 | （例：时间类型选型） | Feign+JSON 序列化踩坑 | 全层统一用 X 类型 |

> 项目纯规则（字段命名、DDL、错误码段）不抄进本表——指针引用 §2 的约束文档，避免双源。
