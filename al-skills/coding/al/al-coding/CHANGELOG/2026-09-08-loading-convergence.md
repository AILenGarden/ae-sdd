# 0.5.2 — 加载确定性与 Review 去重

## 目标与验收

- Coding Core、思想与风险参考、注册记录及适配规范保持原内容。
- 入口明确 Coding / Review / 发现的必读范围、条件读取和截断续读；Review 的 R01–R11 依赖闭合。
- 每条适用规范仍须检查，证据不足不能变为通过。
- Codex 保留插件入口，重复的旧 `.agents/skills/al-coding` 移出发现目录；ZCode 与源正文一致。
- 分发前后核对完整文件集及哈希，旧版本保留在发现目录之外的备份。

## Review 语义承接

| 原内容 | 当前位置 |
|---|---|
| G1 范围、G2 规范 | §1 评审前置及入口读取协议 |
| G3 需求 | §3 A |
| G4 结构 | §3 C |
| G5 风险 | §3 D–F |
| G6 证据 | §4 |
| G7 一致性 | §3 B 的方案、代码、测试、文档和产出物对账 |
| 完成清单与闸门汇总结论 | §2 整体结论，修复后按 §5 重检 |

移除重复闸门清单和完成清单；保留覆盖面、证据规则、问题要素及根因追溯。
区分问题严重度与通过条件：一般级规范违反仍不通过；证据不足列验证限制。
包版本以入口 metadata 和 manifest 的基础版本 0.5.2 为准；Codex 构建后缀由安装工具生成，各 Core 页脚为其内容修订版本。

## 本次验证与分发

- 入口 55 → 47 行；Review 143 → 108 行；Coding Core 145 行不变。
- 15 个运行时文件（含 7 个适配 SKILL）逐文件核验；12 个未修改的正文文件与变更前 SHA-256 一致。
- 源、包、Codex 缓存、ZCode 及其镜像的运行时正文一致；注册表 7 条路径存在，Markdown 文件链接检查通过。
- skill-creator 格式校验和 plugin-creator 包校验通过；Codex CLI 确认 installed, enabled，构建版本为 `0.5.2+codex.20260908095506`。
- 旧 `.agents/skills/al-coding`（含 0.4.3 dist）及源 dist 0.5.1 已移出发现/分发目录，旧缓存由重新安装清理。当前 Codex 仅保留新插件入口，ZCode 仅保留正文包。
- 备份：`C:/Users/EDY/AppData/Local/Temp/al-coding-loading-20260908-01/`。历史文档留在源树和工作区镜像，运行时不分发历史文档及源 manifest。
- 检查属于静态结构、规则承接和安装一致性验证；未运行独立 Agent 行为测试，新会话技能发现与实际读取行为待实测。未提交 Git。

## 后续项目级边界

`icec-cloud-life` 的项目约束以独立的 `scope: project` 适配器注册；Core 和 global 技能不加载 Life 专属约束。适配器只保存项目身份、事实和权威文件指针，具体规则继续由 `D:\Item\life` 文件提供。
