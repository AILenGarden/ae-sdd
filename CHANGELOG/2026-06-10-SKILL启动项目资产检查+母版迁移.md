# 2026-06-10 SKILL 启动时项目资产检查 + 母版迁移至 D:\Item\ae-sdd

## 变更摘要

1. 将 ae-sdd 母版从 `D:\Item\icec-cloud-boss\document\life-team-ai-standards\skills\auto-engineering\skills\ae-sdd` 迁移至 `D:\Item\ae-sdd`，以后以 `D:\Item\ae-sdd` 为权威母版。
2. SKILL 启动时新增"第 0 步：工作区与项目资产检查"——有明确工作区时先检查项目资产，没有则调用 `project-assets-update-skill.md §3` 生成，生成过程明确告知用户。

## 详细变更

### 任务 1：母版迁移

```
旧位置：D:\Item\icec-cloud-boss\document\life-team-ai-standards\skills\auto-engineering\skills\ae-sdd\
新位置：D:\Item\ae-sdd\
操作：cp -rf（保留旧位置不删）
验证：diff -rq 新旧 | wc -l = 0
```

从本次起，`D:\Item\ae-sdd` 是权威母版，所有 SKILL 改动在此进行。

### 任务 2：SKILL 启动项目资产检查

**改动文件：** `D:\Item\ae-sdd\skills\orchestration\ae-sdd-skill.md`

**改动 1 — 路由决策算法加第 0 步**（原 5 步前加 1 步）

```
0. 工作区与项目资产检查
   0.1 判断 projectKey 是否已知
   0.2 调用 get_assets(projectKey) 检查资产
   0.3 资产缺失时：明确告知用户 → 生成资产 → 报告摘要 → 用户确认
```

用户告知模板：
```
⚠️ 未找到项目 {projectKey} 的资产文件（{assetsPath}）。
项目资产包含微服务清单、分层规则、命名约定、工程约束等，
是后续所有流程的上下文基础。
正在为您生成项目资产，这需要扫描工程目录……
```

生成完成模板：
```
✅ 项目资产已生成：{assetsPath}
包含：{microservices 数量} 个微服务 / {分层} 层架构 / {技术栈}
请确认资产内容是否准确，确认后继续流程。
```

**改动 2 — 执行清单加第 0/0a/0b/0c 行**

| # | 动作 | 门禁 |
|---|------|------|
| 0 | 工作区与项目资产检查 | projectKey 已知 + 资产存在或已生成 |
| 0a | 确认 projectKey | 用户明确告知 |
| 0b | 检查资产存在性 | 存在 → 静默通过；不存在 → 0c |
| 0c | 生成资产并告知用户 | 用户确认资产准确 |

### 3 条硬规则

- 🔴 不允许跳过 0.2（即使用户说"直接开始"）
- 🔴 0.3 生成过程必须明确告知用户
- 🟡 资产存在时静默加载，不打扰用户

## 验证

```bash
grep "工作区与项目资产检查" D:/Item/ae-sdd/skills/orchestration/ae-sdd-skill.md
# 期望：路由决策算法 + 执行清单两处均有
```

## Reviewer

- 用户确认 2026-06-10
