# test-tool 评分 skill 依赖
#
# 三个脚本（collect.py / score.py / validate.py）默认只用 Python 标准库，
# 以下依赖都是可选的，装了体验更好，没装会自动降级。
#
# 安装：pip install -r requirements.txt

# JSON Schema 校验（validate.py 优先用，未装则降级为基本结构检查）
jsonschema>=4.20.0

# 代码统计（collect.py 优先用 cloc 的 JSON 输出，未装则降级为内置 wc -l）
# 注意：cloc 是 Perl 写的命令行工具，不是 Python 包，需要单独安装：
#   - Windows: choco install cloc  或  scoop install cloc
#   - macOS:   brew install cloc
#   - Linux:   apt install cloc
# 这里不列 cloc 因为它不是 pip 包。

# 备选代码统计（更快的 Rust 实现，可选）
# tokei 也需要单独安装：cargo install tokei
